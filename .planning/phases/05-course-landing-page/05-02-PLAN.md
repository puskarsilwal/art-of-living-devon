---
phase: 05-course-landing-page
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/components/course/hero-section.tsx
  - src/components/course/program-overview-section.tsx
  - src/components/course/research-stats-section.tsx
  - src/components/course/media-logos-section.tsx
autonomous: true
requirements:
  - COURSE-01
  - COURSE-03

must_haves:
  truths:
    - "Hero section shows full-bleed image with gradient overlay, headline, 1-line description, and Register Now CTA"
    - "Program overview section explains what the Happiness Program is with a secondary CTA"
    - "Research stats block shows 6 stat cards in a 2-row grid with TrendingUp/TrendingDown icons and correct directions"
    - "Media logos section shows CNN, Yoga Journal, Harvard Health Publishing, Washington Post with their actual quotes"
    - "All CTAs link to https://www.artofliving.org/gb-en/courses/art-of-living-part-one in a new tab"
  artifacts:
    - path: "src/components/course/hero-section.tsx"
      provides: "Full-bleed hero with next/image fill, gradient overlay, headline, CTA"
      min_lines: 40
    - path: "src/components/course/program-overview-section.tsx"
      provides: "What is the Happiness Program? section with 3-step flow and secondary CTA"
      min_lines: 30
    - path: "src/components/course/research-stats-section.tsx"
      provides: "6 stat cards grid using courseStats data, TrendingUp/TrendingDown icons"
      min_lines: 40
    - path: "src/components/course/media-logos-section.tsx"
      provides: "4 media logos with quotes below each"
      min_lines: 30
  key_links:
    - from: "src/components/course/research-stats-section.tsx"
      to: "src/lib/data/course-stats"
      via: "named import"
      pattern: "import.*courseStats.*course-stats"
    - from: "src/components/course/hero-section.tsx"
      to: "public/images/course/break-free.webp"
      via: "next/image src prop"
      pattern: "src=.*course.*break-free"
---

<objective>
Build the top four sections of the Happiness Program course page: Hero, Program Overview, Research Stats, and Media Logos. These are the above-the-fold and first-impression sections.

Purpose: These four sections establish the page's visual impact and core credibility message — they are what converts a warm lead who attended the intro talk.
Output: 4 Server Component files in src/components/course/ that are ready to be assembled in page.tsx.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-course-landing-page/05-CONTEXT.md
@.planning/phases/05-course-landing-page/05-RESEARCH.md
@.planning/phases/05-course-landing-page/05-01-SUMMARY.md
@src/app/(landing)/intro/page.tsx
@src/components/intro/hero-section.tsx
@src/components/intro/session-intro-section.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hero section + Program Overview section</name>
  <files>
    src/components/course/hero-section.tsx
    src/components/course/program-overview-section.tsx
  </files>
  <action>
Create src/components/course/ directory and build two Server Components.

**src/components/course/hero-section.tsx**
Full-bleed hero following EXACTLY the same next/image fill + gradient overlay pattern as src/components/intro/hero-section.tsx (established Phase 3 pattern — do not deviate).

Structure:
- Outer `<section>` with `relative overflow-hidden min-h-[70vh] flex items-center`
- Absolute `<div>` containing `<Image src="/images/course/break-free.webp" alt="" fill className="object-cover" priority />`
- Absolute gradient overlay: `bg-gradient-to-b from-black/70 via-black/50 to-black/60`
- Relative z-20 content div: `max-w-4xl mx-auto px-4 py-20 text-center text-white`

Content:
- Small eyebrow badge: "Art of Living Part 1 Course" (using Badge component or styled span)
- H1 headline: "Breathe Out Stress From Day One" (per CONTEXT.md — exact copy locked)
- Subheadline: "Learn Sudarshan Kriya (SKY Breath Meditation) — a powerful breathing technique backed by 100+ peer-reviewed studies. 3 days. Life-changing results."
- Register Now CTA: external anchor wrapping shadcn Button size="lg". Link: https://www.artofliving.org/gb-en/courses/art-of-living-part-one (target="_blank" rel="noopener noreferrer")
- Below CTA: "Free to register • Devon & Southwest • Online & In-Person" in small muted text

Use OKLCH CSS variables (`text-primary`, `bg-primary`) — never inline hex values (project standard from Phase 1).

**src/components/course/program-overview-section.tsx**
"What is the Happiness Program?" section explaining the course overview and 3-step transformation journey.

Structure:
- `bg-background` (white) section, centered text, generous vertical padding
- Section header: "What is the Happiness Program?"
- 2-3 paragraph overview of the Art of Living Part 1 course: 3 days, 3 hours/day, Sudarshan Kriya, pranayama, yoga, and meditation. Mention it is offered by the Art of Living Foundation, founded by Sri Sri Ravi Shankar in 1982.
- 3-step flow (from reference page): (1) "Clear Stress" — release accumulated stress through guided breathing techniques, (2) "Experience Meditation" — access deep states of rest and clarity, (3) "Connect to Inner Peace" — build lasting tools for everyday well-being
- Each step: numbered badge, bold title, 1-sentence description
- Secondary CTA below: "View Upcoming Dates" as an anchor link to `#upcoming-dates` section (smooth scroll, established pattern)

No data import needed — all content is static copy in the component.
  </action>
  <verify>
    <automated>cd "/Users/mac/Documents/Art of Living Landing Page" && npx tsc --noEmit 2>&1 | grep -E "course/hero|course/program" | head -10; echo "exit: $?"</automated>
    <manual>Confirm no TypeScript errors. Verify hero uses next/image fill (not CSS background-image). Verify Register Now CTA uses external link with target="_blank".</manual>
  </verify>
  <done>Both files exist and compile. Hero uses next/image fill with gradient overlay (matching intro page pattern). Program overview has 3-step flow with secondary CTA. All Register Now CTAs use the AoL GB URL with target="_blank".</done>
</task>

<task type="auto">
  <name>Task 2: Research Stats section + Media Logos section</name>
  <files>
    src/components/course/research-stats-section.tsx
    src/components/course/media-logos-section.tsx
  </files>
  <action>
**src/components/course/research-stats-section.tsx**
The credibility centrepiece — 6 stat cards in a 2-row grid with directional arrows.

Structure:
- `bg-muted/30` section (subtle background to distinguish from surrounding white sections)
- Section header: "Backed by 100+ Independent Studies"
- Sub-header: "Peer-reviewed research from Yale, Harvard, AIIMS, and leading universities worldwide"
- Import `courseStats` from `@/lib/data/course-stats`
- Import `TrendingUp` and `TrendingDown` from `lucide-react`
- Grid: `grid grid-cols-2 sm:grid-cols-3 gap-6 sm:gap-8 max-w-3xl mx-auto`
- Each stat card: centered, no border/card container — just the stat data displayed prominently
  - Large percentage: `text-4xl sm:text-5xl font-bold text-primary`
  - Icon + percentage on same row: flex items-center justify-center gap-2
  - TrendingUp for direction="up", TrendingDown for direction="down" — both in `text-primary`, h-6 w-6
  - Label below: `text-sm font-medium text-foreground`
  - Timeframe below label: `text-xs text-muted-foreground` (only if timeframe is non-empty)

Do NOT add Card wrappers — the reference page uses clean unboxed stats for maximum visual impact.

**src/components/course/media-logos-section.tsx**
Press credibility bar with actual quotes from each outlet. CONTEXT.md says "use actual quotes from the reference" and research notes actual logo files are available.

Structure:
- `bg-background` section, moderate vertical padding
- Section header: "As Featured In" (small, centered, muted — logos are the star)
- 4-column grid on md+, 2-column on mobile: CNN, Yoga Journal, Harvard Health Publishing, The Washington Post
- Each item: logo image using `<Image>` from next/image (not `<img>`), then quote text below in italic small text

Use the actual image files now in public/images/course/:
- CNN: /images/course/cnn.svg (use `<Image src="/images/course/cnn.svg" alt="CNN" width={80} height={32} />`)
- Harvard: /images/course/harvard.webp (width={120} height={40})
- Yoga Journal: /images/course/yoga-journal.webp (width={120} height={40})
- Washington Post: /images/course/washington-post.webp (width={140} height={40})

If any logo file was missing after Plan 01 (check 05-01-SUMMARY.md), fall back to styled text logos following the intro page MediaLogosSection pattern (font-black for CNN, font-serif for WashPost, etc.).

Quotes below each logo (from CONTEXT.md reference):
- CNN: "Breathing technique that can change your life"
- Yoga Journal: "One of the world's leading wellness organizations"
- Harvard Health Publishing: "Research-supported benefits of breathing practices"
- The Washington Post: "The breath as medicine"

Each quote in `text-xs italic text-muted-foreground text-center mt-2`.
  </action>
  <verify>
    <automated>cd "/Users/mac/Documents/Art of Living Landing Page" && npx tsc --noEmit 2>&1 | grep -E "course/research|course/media" | head -10; echo "exit: $?"</automated>
    <manual>Confirm courseStats import resolves. Verify TrendingUp used for calm (37%) and social connection (25%), TrendingDown for the other 4. Confirm each media entry has a quote below it.</manual>
  </verify>
  <done>Both files exist and compile. ResearchStatsSection imports courseStats and maps with correct TrendingUp/TrendingDown logic. MediaLogosSection renders 4 outlets with quote text under each. No TypeScript errors.</done>
</task>

</tasks>

<verification>
Run after all tasks complete:
1. `npx tsc --noEmit` — no errors in course/hero-section.tsx or course/research-stats-section.tsx
2. Check hero-section.tsx contains `fill` prop on Image (not CSS background)
3. Check research-stats-section.tsx: `grep "direction.*up" src/lib/data/course-stats.ts` returns calm and social connection entries
4. Check all CTA hrefs point to `artofliving.org/gb-en`
</verification>

<success_criteria>
- 4 Server Component files exist in src/components/course/
- Hero uses next/image fill + gradient overlay (not CSS background-image)
- Research stats grid has 6 items with correct TrendingUp/Down icons matching course-stats.ts directions
- All Register Now buttons are external links to the AoL GB registration URL with target="_blank"
- npm run build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/05-course-landing-page/05-02-SUMMARY.md`
</output>
