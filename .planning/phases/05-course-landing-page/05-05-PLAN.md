---
phase: 05-course-landing-page
plan: "05"
type: execute
wave: 3
depends_on:
  - "05-02"
  - "05-03"
  - "05-04"
files_modified:
  - src/app/(landing)/happiness-program/page.tsx
autonomous: false
requirements:
  - COURSE-01
  - COURSE-02
  - COURSE-03
  - COURSE-04
  - COURSE-05

must_haves:
  truths:
    - "Visiting /happiness-program renders the complete 11-section course landing page"
    - "Page title in browser tab reads 'Happiness Program | Art of Living Devon & Southwest'"
    - "All 11 sections appear in the correct order top to bottom"
    - "No broken images — hero, founder, testimonials all render"
    - "All Register Now CTAs open the AoL registration page in a new tab"
    - "FAQ accordion expands and collapses on click"
    - "Page is mobile-responsive (no horizontal overflow at 375px)"
  artifacts:
    - path: "src/app/(landing)/happiness-program/page.tsx"
      provides: "Course landing page assembler importing all 11 section components"
      min_lines: 35
      exports: ["default HappinessProgramPage", "metadata"]
  key_links:
    - from: "src/app/(landing)/happiness-program/page.tsx"
      to: "src/components/course/hero-section.tsx"
      via: "named import"
      pattern: "import.*HeroSection.*course/hero-section"
    - from: "src/app/(landing)/happiness-program/page.tsx"
      to: "src/components/course/faq-section.tsx"
      via: "named import"
      pattern: "import.*FaqSection.*course/faq-section"
---

<objective>
Assemble the complete Happiness Program course landing page by composing all 11 section components into the page.tsx file at /happiness-program, then verify the full page visually.

Purpose: The final wiring step that makes the course landing page live and navigable.
Output: A working course landing page at /happiness-program — the last deliverable of Phase 5.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-course-landing-page/05-CONTEXT.md
@.planning/phases/05-course-landing-page/05-RESEARCH.md
@.planning/phases/05-course-landing-page/05-01-SUMMARY.md
@.planning/phases/05-course-landing-page/05-02-SUMMARY.md
@.planning/phases/05-course-landing-page/05-03-SUMMARY.md
@.planning/phases/05-course-landing-page/05-04-SUMMARY.md
@src/app/(landing)/intro/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble page.tsx with all 11 sections and metadata</name>
  <files>
    src/app/(landing)/happiness-program/page.tsx
  </files>
  <action>
Create the directory and page file: `src/app/(landing)/happiness-program/page.tsx`

This is a pure assembler — no logic, no state, no data fetching. It imports all 11 section components and composes them in the LOCKED order from CONTEXT.md.

```typescript
import type { Metadata } from "next"
import { HeroSection } from "@/components/course/hero-section"
import { ProgramOverviewSection } from "@/components/course/program-overview-section"
import { ResearchStatsSection } from "@/components/course/research-stats-section"
import { MediaLogosSection } from "@/components/course/media-logos-section"
import { CourseContentSection } from "@/components/course/course-content-section"
import { UpcomingDatesSection } from "@/components/course/upcoming-dates-section"
import { FounderSection } from "@/components/course/founder-section"
import { TestimonialsSection } from "@/components/course/testimonials-section"
import { NumbersSection } from "@/components/course/numbers-section"
import { FaqSection } from "@/components/course/faq-section"
import { FooterCta } from "@/components/course/footer-cta"

export const metadata: Metadata = {
  title: "Happiness Program | Art of Living Devon & Southwest",
  description: "Transform stress into calm in 3 days. Learn Sudarshan Kriya (SKY Breath Meditation), pranayama, and yoga — backed by 100+ peer-reviewed studies. Available in Exeter, Devon and online.",
  robots: { index: true, follow: true },
}

export default function HappinessProgramPage() {
  return (
    <>
      <HeroSection />
      <ProgramOverviewSection />
      <ResearchStatsSection />
      <MediaLogosSection />
      <CourseContentSection />
      <UpcomingDatesSection />
      <FounderSection />
      <TestimonialsSection />
      <NumbersSection />
      <FaqSection />
      <FooterCta />
    </>
  )
}
```

Match the actual named exports from each component file — check the component files to confirm export names (e.g., FooterCta vs FooterCTA — match exactly).

The page sits inside the `(landing)` route group which has no header/footer layout (established Phase 2 pattern). This is the correct route group — do NOT use `(main)`.

After creating the file, run `npm run build` and fix any import errors before the checkpoint.
  </action>
  <verify>
    <automated>cd "/Users/mac/Documents/Art of Living Landing Page" && npm run build 2>&1 | tail -15</automated>
    <manual>Confirm npm run build exits 0. Check for any TypeScript import errors from mismatched export names.</manual>
  </verify>
  <done>src/app/(landing)/happiness-program/page.tsx exists, npm run build exits 0, no TypeScript import errors. Page is accessible at /happiness-program in development mode.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete course landing page</name>
  <files>src/app/(landing)/happiness-program/page.tsx</files>
  <action>
Run `npm run dev` and open http://localhost:3000/happiness-program to verify the complete course landing page. This is a visual inspection checkpoint — no code changes expected unless issues are found.
  </action>
  <verify>
    <automated>cd "/Users/mac/Documents/Art of Living Landing Page" && npm run build 2>&1 | grep -E "error|Error" | grep -v node_modules | head -5; echo "exit: $?"</automated>
    <manual>
1. Run: `npm run dev`
2. Open: http://localhost:3000/happiness-program
3. Check section order matches: Hero → Program Overview → Research Stats → Media Logos → Course Content → Upcoming Dates → Founder → Testimonials → Numbers → FAQ → Footer CTA
4. Scroll through the full page — no broken images, no layout overflow
5. Click "Register Now" in the hero — should open artofliving.org/gb-en in a new tab
6. Click any "Register Now" in the Upcoming Dates cards — should open artofliving.org/gb-en in a new tab
7. Click a FAQ question — it should expand and collapse
8. Resize to 375px width (mobile) — no horizontal scrollbar, all text readable
9. Confirm the testimonials section header reads "How is Art of Living Changing Lives?"
10. Confirm the research stats show correct arrows: 37% calm has UP arrow, 23% anxiety has DOWN arrow
    </manual>
  </verify>
  <done>Human has visually confirmed all 11 sections render correctly, CTAs work, FAQ accordion works, mobile layout is clean.</done>
</task>

</tasks>

<verification>
1. `npm run build` exits 0 — page.tsx compiles, all imports resolve
2. http://localhost:3000/happiness-program loads without errors
3. All 11 sections render in correct order
4. Register Now CTAs open AoL registration page in new tab
5. FAQ accordion works
6. Mobile viewport (375px) has no horizontal overflow
</verification>

<success_criteria>
Phase 5 complete when:
- /happiness-program renders all 11 sections in correct order (Hero → FooterCTA)
- npm run build exits 0
- All Register Now CTAs link to https://www.artofliving.org/gb-en/courses/art-of-living-part-one in a new tab
- FAQ accordion is interactive
- Page is mobile-responsive at 375px
- Human has approved visual appearance
</success_criteria>

<output>
After completion, create `.planning/phases/05-course-landing-page/05-05-SUMMARY.md`
</output>
