---
phase: 05-course-landing-page
plan: "04"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/components/course/testimonials-section.tsx
  - src/components/course/numbers-section.tsx
  - src/components/course/faq-section.tsx
  - src/components/course/footer-cta.tsx
autonomous: true
requirements:
  - COURSE-05

must_haves:
  truths:
    - "Testimonials section header reads 'How is Art of Living Changing Lives?' with 3 course participant testimonials"
    - "Numbers section shows Art of Living global reach stats: 44 years, 180+ countries, 40,000+ teachers, 800M+ people"
    - "FAQ section has 4-6 collapsible questions using shadcn Accordion"
    - "Footer CTA is the final Register Now section visible at the bottom of the page"
    - "Testimonial photos use /images/course/testimonials/ paths (not intro page paths)"
  artifacts:
    - path: "src/components/course/testimonials-section.tsx"
      provides: "3 course testimonials using courseTestimonials data, circular photos, transformation-focused"
      min_lines: 40
    - path: "src/components/course/numbers-section.tsx"
      provides: "Art of Living in numbers: 4 stats in responsive grid"
      min_lines: 25
    - path: "src/components/course/faq-section.tsx"
      provides: "FAQ with shadcn Accordion (use client), 4-6 questions"
      min_lines: 40
    - path: "src/components/course/footer-cta.tsx"
      provides: "Final Register Now CTA section"
      min_lines: 20
  key_links:
    - from: "src/components/course/testimonials-section.tsx"
      to: "src/lib/data/course-testimonials"
      via: "named import"
      pattern: "import.*courseTestimonials.*course-testimonials"
    - from: "src/components/course/faq-section.tsx"
      to: "src/components/ui/accordion"
      via: "named import"
      pattern: "import.*Accordion.*accordion"
---

<objective>
Build the final four sections of the Happiness Program page: Testimonials, Art of Living in Numbers, FAQ accordion, and Footer CTA.

Purpose: These sections close the sale — social proof, scale/authority, addressing remaining objections via FAQ, and a final conversion moment.
Output: 4 component files, including the only client component in the whole page (FaqSection).
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-course-landing-page/05-CONTEXT.md
@.planning/phases/05-course-landing-page/05-RESEARCH.md
@.planning/phases/05-course-landing-page/05-01-SUMMARY.md
@src/components/intro/testimonials-section.tsx
@src/components/intro/credibility-section.tsx
@src/components/intro/footer-cta.tsx
@src/lib/data/course-testimonials.ts
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Testimonials section + Numbers section</name>
  <files>
    src/components/course/testimonials-section.tsx
    src/components/course/numbers-section.tsx
  </files>
  <action>
**src/components/course/testimonials-section.tsx**
Reuses the visual pattern of src/components/intro/testimonials-section.tsx but with course-specific data and copy.

Section header (LOCKED per CONTEXT.md — do not change): "How is Art of Living Changing Lives?"

Import `courseTestimonials` from `@/lib/data/course-testimonials`.

Layout: Responsive grid, 1 col mobile, md:grid-cols-3. Each testimonial card:
- Circular photo: `<Image src={t.imagePath} alt={t.name} width={80} height={80} className="rounded-full object-cover mx-auto mb-3" />`
- Full testimonial quote in italics, wrapped in blockquote with `text-muted-foreground`
- Name: `font-semibold text-foreground`
- Role/credentials: `text-sm text-muted-foreground` (use t.role if available from the Testimonial type)

Use Card component for each testimonial, matching the intro testimonials pattern.
Background: `bg-muted/30` to distinguish from surrounding white sections.

Do NOT import from src/lib/data/testimonials.ts — use course-testimonials.ts exclusively (separate audience, separate data file, per RESEARCH.md pitfall 2).

**src/components/course/numbers-section.tsx**
"Art of Living in Numbers" — global reach stats. Server Component, no imports needed beyond standard.

Section header: "Art of Living in Numbers"
Sub-header: "44 years of transforming lives around the world"

4 stats in a 2x2 grid (mobile), 4-column on lg (same breakpoint pattern as AboutSection — lg:grid-cols-4):
- "44+" — "Years of teaching" (founded 1982)
- "180+" — "Countries worldwide"
- "40,000+" — "Certified teachers"
- "800M+" — "People reached"

Each stat: large bold number in `text-primary` (text-4xl font-bold), label below in `text-sm text-muted-foreground`.
Section background: dark full-bleed, matching `bg-gray-950` (same as intro CredibilitySection dark treatment). Text on dark: text-white for numbers, text-gray-300 for labels.

This creates visual contrast between sections — the dark numbers section breaks up the white/light sections above and below it.
  </action>
  <verify>
    <automated>cd "/Users/mac/Documents/Art of Living Landing Page" && npx tsc --noEmit 2>&1 | grep -E "course/testimonials|course/numbers" | head -10; echo "exit: $?"</automated>
    <manual>Confirm testimonials-section.tsx imports from course-testimonials (NOT from testimonials.ts). Verify section header reads "How is Art of Living Changing Lives?" exactly. Confirm numbers section has 4 stats.</manual>
  </verify>
  <done>Both files exist and compile. TestimonialsSection imports from course-testimonials and has correct section header. NumbersSection shows 4 Art of Living global stats on dark background.</done>
</task>

<task type="auto">
  <name>Task 2: FAQ section (client component) + Footer CTA</name>
  <files>
    src/components/course/faq-section.tsx
    src/components/course/footer-cta.tsx
  </files>
  <action>
**src/components/course/faq-section.tsx**
The ONLY client component on the entire course page. Must have `"use client"` at the top.

Check that src/components/ui/accordion.tsx exists (installed in Plan 01). Import from it:
```typescript
"use client"
import {
  Accordion,
  AccordionContent,
  AccordionItem,
  AccordionTrigger,
} from "@/components/ui/accordion"
```

FAQ questions (Claude's discretion per CONTEXT.md — write 5 comprehensive questions):

1. **How long is the Happiness Program?** — 3 days, approximately 3 hours each day (morning or evening sessions available). Total time commitment is 9 hours over 3 consecutive days.

2. **How much does the course cost?** — The course fee varies by location and format. Visit the Art of Living registration page to see current pricing for Devon & Southwest courses. Financial assistance may be available — contact your local centre.

3. **Is it available online or in-person?** — Both formats are offered. In-person courses take place in Exeter, Devon. Online courses are delivered over Zoom from the comfort of your home. Both formats teach the full SKY Breath Meditation technique.

4. **Do I need any prior experience?** — No experience is needed. The Happiness Program is designed for complete beginners. You do not need any yoga, meditation, or breathwork background.

5. **What if I miss a session?** — Attendance at all 3 sessions is important for the complete learning experience. If you need to reschedule, contact your course teacher as early as possible — make-up options may be available depending on the format.

Layout:
- `bg-background` section with generous vertical padding
- Max width: `max-w-2xl mx-auto` (narrow for readability)
- Section header: "Frequently Asked Questions"
- `<Accordion type="single" collapsible className="w-full">`
- Each FAQ: `<AccordionItem value={"item-" + i}>` with `<AccordionTrigger>` for question and `<AccordionContent>` for answer

**src/components/course/footer-cta.tsx**
Final conversion moment at the bottom of the page. Server Component (no "use client" needed).

Structure (mirrors src/components/intro/footer-cta.tsx pattern but no background image needed):
- `bg-primary` section (orange brand color) for maximum contrast
- Centered text, generous vertical padding
- H2: "Ready to Transform Your Life?"
- Subtext: "Join thousands of people who have discovered the power of SKY Breath Meditation. 3 days could change everything."
- Large Register Now CTA: external anchor wrapping `<Button size="lg" variant="secondary">` (white button on orange background)
- Link: https://www.artofliving.org/gb-en/courses/art-of-living-part-one (target="_blank" rel="noopener noreferrer")
- Below button: "Free to register • No experience needed • Online & In-Person available" in small text with opacity

Use `text-primary-foreground` for text on the primary background — do not hardcode white, use the OKLCH variable.
  </action>
  <verify>
    <automated>cd "/Users/mac/Documents/Art of Living Landing Page" && npx tsc --noEmit 2>&1 | grep -E "course/faq|course/footer" | head -10; echo "exit: $?"</automated>
    <manual>Confirm faq-section.tsx has "use client" at line 1. Confirm it imports from @/components/ui/accordion (not a different path). Verify footer-cta.tsx does NOT have "use client" (Server Component). Verify footer CTA link is external AoL GB URL.</manual>
  </verify>
  <done>faq-section.tsx has "use client" directive and uses shadcn Accordion with 5 questions. footer-cta.tsx is a Server Component with Register Now external link. Both compile without errors.</done>
</task>

</tasks>

<verification>
After all tasks:
1. `grep -r "use client" src/components/course/` — only faq-section.tsx should appear
2. `grep "course-testimonials" src/components/course/testimonials-section.tsx` — import confirmed
3. `npx tsc --noEmit` — zero errors in course/ components
4. `npm run build` — exits 0
</verification>

<success_criteria>
- 4 component files exist in src/components/course/
- Only faq-section.tsx has "use client" — all others are Server Components
- TestimonialsSection imports from course-testimonials (not intro testimonials)
- Section header is exactly "How is Art of Living Changing Lives?"
- Footer CTA links to AoL GB registration URL with target="_blank"
- npm run build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/05-course-landing-page/05-04-SUMMARY.md`
</output>
