---
phase: 03-intro-talk-landing-page-trust-content
plan: 08
type: execute
wave: 2
depends_on: ["07"]
files_modified:
  - src/components/intro/benefits-section.tsx
  - src/components/intro/credibility-section.tsx
  - src/components/intro/about-section.tsx
  - src/app/(landing)/intro/page.tsx
autonomous: true
gap_closure: true
requirements:
  - INTRO-07
  - INTRO-08

must_haves:
  truths:
    - "Benefits section shows 3 circular photo cards using real lifestyle images instead of abstract icon cards"
    - "Credibility section is a dark dramatic 2-column section with lifestyle image on left and research narrative on right"
    - "An About section exists after TeacherSection explaining the Art of Living organization (40+ years, Gurudev Sri Sri Ravi Shankar, 180 countries, volunteer non-profit)"
  artifacts:
    - path: "src/components/intro/benefits-section.tsx"
      provides: "3-card grid with circular photo crops, captions, and descriptions"
      contains: "rounded-full"
    - path: "src/components/intro/credibility-section.tsx"
      provides: "Dark 2-column section: image left with bold headline, stats + narrative right"
      contains: "bg-gray-950"
    - path: "src/components/intro/about-section.tsx"
      provides: "Organization backstory section with lifestyle image and founding narrative"
      exports: ["AboutSection"]
    - path: "src/app/(landing)/intro/page.tsx"
      provides: "Wired composition with AboutSection after TeacherSection"
      contains: "AboutSection"
  key_links:
    - from: "src/components/intro/benefits-section.tsx"
      to: "public/images/intro/breathing-session.jpg, meditation-group.jpg, teacher-guiding.jpg"
      via: "next/image src props on circular image containers"
      pattern: "images/intro/"
    - from: "src/components/intro/credibility-section.tsx"
      to: "public/images/intro/meditation-group.jpg"
      via: "next/image fill in left column"
      pattern: "meditation-group.jpg"
    - from: "src/app/(landing)/intro/page.tsx"
      to: "src/components/intro/about-section.tsx"
      via: "named import + JSX after TeacherSection"
      pattern: "AboutSection"
---

<objective>
Close gaps 14, 16, and 17 from the human review against the Art of Living reference page.

Gap 14: BenefitsSection shows 4 abstract lucide-react icon cards. The reference uses 3 circular photos of real people doing breathing practices. Human photos communicate "real people get results" far more powerfully than icons. We already have 3 usable lifestyle images on disk (breathing-session.jpg, meditation-group.jpg, teacher-guiding.jpg).

Gap 16: CredibilitySection is `bg-muted/50` with stats in cards — functional but forgettable. The reference has a dark full-bleed 2-column section: lifestyle image with bold headline on the left, Yale research narrative text on the right. The current design lacks the emotional weight the reference achieves.

Gap 17: No About section exists. Cold Facebook traffic has zero context for who Art of Living is. Without organizational backstory (40+ years, Gurudev Sri Sri Ravi Shankar founding, volunteer non-profit, 180 countries) visitors are being asked to register for an event by a complete stranger.

Purpose: Shift from icon-and-stats page to a story-driven, human page. Benefits feel human (photos). Credibility feels dramatic (dark full-bleed). Organization feels trustworthy and established (About section).

Output: Rewritten benefits-section.tsx, rewritten credibility-section.tsx, new about-section.tsx, updated page.tsx.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-intro-talk-landing-page-trust-content/03-CONTEXT.md
@src/app/(landing)/intro/page.tsx
@src/components/intro/benefits-section.tsx
@src/components/intro/credibility-section.tsx
@src/lib/data/credibility-stats.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite BenefitsSection — 3 circular photo cards replacing icon cards</name>
  <files>src/components/intro/benefits-section.tsx</files>
  <action>
Completely rewrite `src/components/intro/benefits-section.tsx`. Replace the 4-icon-card grid with 3 circular photo cards using existing disk images, matching the reference page's human approach.

The 3 images available on disk:
- `/images/intro/breathing-session.jpg` — group doing breathing practice
- `/images/intro/meditation-group.jpg` — group meditating together
- `/images/intro/teacher-guiding.jpg` — teacher guiding a session

Full replacement implementation:

```tsx
import Image from "next/image"
import { Button } from "@/components/ui/button"

const benefits = [
  {
    image: "/images/intro/breathing-session.jpg",
    alt: "People learning SKY Breath Meditation in a group session",
    caption: "SKY Breath Meditation",
    description:
      "A powerful rhythmic breathing technique backed by Yale and Harvard research — reduces stress and anxiety in a single session",
  },
  {
    image: "/images/intro/meditation-group.jpg",
    alt: "Group meditating together in an Art of Living session",
    caption: "Deep Rest & Better Sleep",
    description:
      "Guided practices that calm the nervous system, deepen sleep quality, and restore natural energy — without years of training",
  },
  {
    image: "/images/intro/teacher-guiding.jpg",
    alt: "Certified Art of Living teacher guiding participants",
    caption: "Expert Guidance, Warm Community",
    description:
      "Certified Art of Living teachers and a welcoming Devon & Southwest community to support your journey every step of the way",
  },
]

export function BenefitsSection() {
  return (
    <section className="px-4 py-12 sm:px-6 sm:py-16 lg:py-20 bg-muted/50">
      <div className="max-w-4xl mx-auto">
        <div className="text-center mb-10">
          <p className="text-sm font-semibold text-primary uppercase tracking-wider mb-2">
            What You'll Experience
          </p>
          <h2 className="text-xl font-bold tracking-tight sm:text-2xl md:text-3xl">
            Real Techniques. Real Results. Real People.
          </h2>
        </div>

        <div className="grid grid-cols-1 sm:grid-cols-3 gap-8 sm:gap-6 mb-10">
          {benefits.map((benefit) => (
            <div key={benefit.caption} className="flex flex-col items-center text-center">
              {/* Circular photo */}
              <div className="relative w-40 h-40 sm:w-36 sm:h-36 md:w-44 md:h-44 rounded-full overflow-hidden mb-4 shadow-lg ring-4 ring-primary/20">
                <Image
                  src={benefit.image}
                  alt={benefit.alt}
                  fill
                  className="object-cover"
                  sizes="(max-width: 640px) 160px, (max-width: 768px) 144px, 176px"
                />
              </div>
              <h3 className="font-semibold text-base sm:text-lg mb-2">
                {benefit.caption}
              </h3>
              <p className="text-sm sm:text-base text-muted-foreground leading-relaxed max-w-xs">
                {benefit.description}
              </p>
            </div>
          ))}
        </div>

        <div className="text-center">
          <a href="#register">
            <Button
              size="lg"
              variant="outline"
              className="border-primary text-primary hover:bg-primary hover:text-primary-foreground w-full sm:w-auto h-14 text-lg font-semibold px-8"
            >
              Save My Seat — It&apos;s Free
            </Button>
          </a>
        </div>
      </div>
    </section>
  )
}
```

Remove the old imports: `Wind`, `Moon`, `Users`, `GraduationCap` from lucide-react, `Card`, `CardContent` from shadcn/ui. Add `Image` from `next/image`. Keep `Button` import unchanged.

The section background stays `bg-muted/50` to maintain the existing page rhythm (hero dark → session-intro white → benefits muted → credibility dark).
  </action>
  <verify>
    <automated>cd "/Users/mac/Documents/Art of Living Landing Page" && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Open /intro — benefits section should show 3 circular photos in a row (or stacked on mobile) with captions below, not icon cards. The old Wind/Moon/Users/GraduationCap icons should be gone.</manual>
  </verify>
  <done>Benefits section shows 3 circular photo cards using breathing-session.jpg, meditation-group.jpg, and teacher-guiding.jpg. No lucide-react icon imports remain in the file. TypeScript compiles with zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite CredibilitySection — dark dramatic 2-column layout</name>
  <files>src/components/intro/credibility-section.tsx</files>
  <action>
Completely rewrite `src/components/intro/credibility-section.tsx`. Transform it from a light card-grid section into a dark full-bleed 2-column section matching the reference's visual impact.

Layout: dark section (`bg-gray-950`), left column has lifestyle photo with bold headline overlay, right column has stats + research narrative.

Retain the `credibilityStats` and `researchHighlights` data imports from `@/lib/data/credibility-stats` for the right column stats.

Full replacement implementation:

```tsx
import Image from "next/image"
import { credibilityStats, researchHighlights } from "@/lib/data/credibility-stats"

export function CredibilitySection() {
  return (
    <section className="bg-gray-950 text-white">
      <div className="max-w-6xl mx-auto">
        <div className="grid grid-cols-1 lg:grid-cols-2 min-h-[480px]">

          {/* Left column: lifestyle image with bold overlay headline */}
          <div className="relative min-h-[280px] lg:min-h-full overflow-hidden">
            <Image
              src="/images/intro/meditation-group.jpg"
              alt="Group meditating in an Art of Living session"
              fill
              className="object-cover opacity-60"
              sizes="(max-width: 1024px) 100vw, 50vw"
            />
            {/* Dark gradient to make text legible */}
            <div className="absolute inset-0 bg-gradient-to-r from-gray-950/80 via-gray-950/50 to-transparent lg:bg-gradient-to-r lg:from-transparent lg:via-gray-950/20 lg:to-gray-950/80" />
            <div className="absolute inset-0 bg-gradient-to-t from-gray-950/90 via-gray-950/40 to-transparent" />

            {/* Overlay headline */}
            <div className="absolute bottom-0 left-0 right-0 p-6 sm:p-8 lg:p-10">
              <p className="text-primary text-sm font-semibold uppercase tracking-widest mb-2">
                Science-Backed. Globally Practised.
              </p>
              <h2 className="text-2xl sm:text-3xl lg:text-4xl font-bold leading-tight text-white">
                Backed by 100+ independent peer-reviewed journals
              </h2>
            </div>
          </div>

          {/* Right column: stats + research narrative */}
          <div className="px-6 py-10 sm:px-8 sm:py-12 lg:px-12 lg:py-14 flex flex-col justify-center">
            {/* Large stats */}
            <div className="grid grid-cols-2 gap-4 mb-8">
              {credibilityStats.map((stat) => (
                <div key={stat.label} className="text-center">
                  <div
                    className={`font-bold text-primary mb-1 ${
                      stat.isText ? "text-xl sm:text-2xl" : "text-3xl sm:text-4xl lg:text-5xl"
                    }`}
                  >
                    {stat.number}
                  </div>
                  <div className="text-xs sm:text-sm text-gray-400 leading-snug">
                    {stat.label}
                  </div>
                </div>
              ))}
            </div>

            {/* Divider */}
            <div className="border-t border-white/10 mb-6" />

            {/* Research highlights narrative */}
            <div className="space-y-4">
              <p className="text-gray-300 text-sm sm:text-base leading-relaxed">
                Universities including Yale, Harvard, and Stanford have published independent research confirming that SKY Breath Meditation significantly reduces:
              </p>
              <ul className="space-y-2">
                {researchHighlights.map((research) => (
                  <li key={research.institution} className="flex items-start gap-2">
                    <span className="text-primary shrink-0 mt-0.5">▸</span>
                    <span className="text-gray-300 text-sm leading-snug">
                      <strong className="text-white">{research.institution}:</strong>{" "}
                      {research.finding}
                    </span>
                  </li>
                ))}
              </ul>
            </div>

            {/* CTA */}
            <div className="mt-8">
              <a href="#register">
                <button className="w-full sm:w-auto px-8 h-12 rounded-lg bg-primary text-primary-foreground font-semibold text-base hover:bg-primary/90 transition-colors">
                  Experience It For Free
                </button>
              </a>
            </div>
          </div>

        </div>
      </div>
    </section>
  )
}
```

Remove old imports: `Users`, `Globe`, `BookOpen`, `GraduationCap`, `FlaskConical` from lucide-react, `Badge` from shadcn/ui, `Button` from shadcn/ui. Add `Image` from `next/image`. Keep `credibilityStats` and `researchHighlights` data imports — these data files are unchanged.

The `iconMap` object can be deleted entirely since the new design doesn't use icons.

Note: The section uses `bg-gray-950` (near-black) which is a standard Tailwind color. No custom CSS needed.
  </action>
  <verify>
    <automated>cd "/Users/mac/Documents/Art of Living Landing Page" && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Open /intro — credibility section should be a dark (near-black) section with the meditation-group.jpg image visible on the left at ~60% opacity, bold headline overlaid, and stats + research findings in white/gray text on the right. The section should stand out dramatically against the light sections before and after it.</manual>
  </verify>
  <done>CredibilitySection renders as a dark `bg-gray-950` 2-column section. Left column shows meditation-group.jpg with opacity-60 and headline overlay. Right column shows credibility stats grid and research narrative list. Old icon imports and Badge are removed. TypeScript compiles with zero errors.</done>
</task>

<task type="auto">
  <name>Task 3: Create AboutSection organization backstory + wire into page.tsx</name>
  <files>
    src/components/intro/about-section.tsx
    src/app/(landing)/intro/page.tsx
  </files>
  <action>
**Part A — Create AboutSection**

Create `src/components/intro/about-section.tsx`. This section tells cold Facebook traffic who Art of Living IS — the founding story, global reach, and volunteer mission — before asking them to register.

```tsx
import Image from "next/image"

const organizationFacts = [
  { number: "40+", label: "Years of teaching" },
  { number: "180", label: "Countries worldwide" },
  { number: "500M+", label: "Lives touched" },
  { number: "10,000+", label: "Volunteer teachers" },
]

export function AboutSection() {
  return (
    <section className="px-4 py-12 sm:px-6 sm:py-16 lg:py-20 bg-muted/30">
      <div className="max-w-5xl mx-auto">
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-12 items-center">

          {/* Left: text content */}
          <div>
            <p className="text-sm font-semibold text-primary uppercase tracking-wider mb-3">
              About the Art of Living
            </p>
            <h2 className="text-2xl font-bold tracking-tight sm:text-3xl md:text-4xl mb-4">
              40 Years of Bringing{" "}
              <span className="text-primary">Peace to the World</span>
            </h2>
            <p className="text-muted-foreground text-base sm:text-lg leading-relaxed mb-4">
              The Art of Living Foundation was founded in 1981 by Gurudev Sri Sri Ravi Shankar — a humanitarian, spiritual teacher, and peace ambassador. What began as a small programme to share powerful breathing techniques has grown into one of the world's largest volunteer-run non-profit organisations.
            </p>
            <p className="text-muted-foreground text-base leading-relaxed mb-6">
              Today, Art of Living operates in 180 countries and has touched over 500 million lives — not through advertising, but through direct teaching, word of mouth, and the genuine transformation people experience in its programmes. Every local session you attend is run by a volunteer certified teacher who has completed the same training.
            </p>

            {/* Org fact grid */}
            <div className="grid grid-cols-2 sm:grid-cols-4 gap-4">
              {organizationFacts.map((fact) => (
                <div key={fact.label} className="text-center rounded-xl bg-background p-4 shadow-sm border border-border/50">
                  <div className="text-2xl sm:text-3xl font-bold text-primary mb-1">
                    {fact.number}
                  </div>
                  <div className="text-xs text-muted-foreground leading-tight">
                    {fact.label}
                  </div>
                </div>
              ))}
            </div>
          </div>

          {/* Right: lifestyle image */}
          <div className="relative aspect-[4/3] rounded-2xl overflow-hidden shadow-xl order-first lg:order-last">
            <Image
              src="/images/intro/teacher-guiding.jpg"
              alt="Art of Living certified teacher guiding a breathing session"
              fill
              className="object-cover"
              sizes="(max-width: 1024px) 100vw, 50vw"
            />
          </div>

        </div>
      </div>
    </section>
  )
}
```

Uses `teacher-guiding.jpg` for the image (teacher in action = organizational credibility). Background `bg-muted/30` creates a subtle distinction from `bg-muted/50` used in benefits — keeps visual variety without introducing new colors.

**Part B — Wire AboutSection into page.tsx**

Update `src/app/(landing)/intro/page.tsx`:

1. Add import after TeacherSection import:
   ```tsx
   import { AboutSection } from "@/components/intro/about-section"
   ```

2. Insert `<AboutSection />` AFTER `<TeacherSection />` and BEFORE `<ObjectionsSection />`.

3. Update the inline comment to add step 8: "About — organizational credibility and founding story" and renumber Objections to 9, Registration to 10, Footer to 11.

Final section order in JSX:
```tsx
<HeroSection />
<SessionIntroSection />        {/* from plan 03-07 */}
<BenefitsSection />
<CredibilitySection />
<MediaLogosSection />          {/* from plan 03-07 */}
<TestimonialsSection />
<WhatToExpectSection />
<TeacherSection />
<AboutSection />               {/* NEW */}
<ObjectionsSection />
<RegistrationForm />
<FooterCTA />
```

Note: SessionIntroSection and MediaLogosSection imports will already be present if plan 03-07 was executed first. If executing in parallel, add those imports here too (they don't conflict since plan 03-07 touches different files). The only file both plans touch is page.tsx — coordinate by having the executor merge the two sets of imports and JSX insertions.

If executed AFTER plan 03-07, only the `AboutSection` import and JSX insertion are new additions.
  </action>
  <verify>
    <automated>cd "/Users/mac/Documents/Art of Living Landing Page" && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -20</automated>
    <manual>Open /intro and scroll past TeacherSection. AboutSection should appear with "About the Art of Living" eyebrow text, "40 Years of Bringing Peace to the World" heading, two narrative paragraphs about Gurudev and the foundation, a 4-fact grid (40+, 180, 500M+, 10,000+), and teacher-guiding.jpg on the right.</manual>
  </verify>
  <done>
    - `src/components/intro/about-section.tsx` exists and exports `AboutSection`
    - `src/app/(landing)/intro/page.tsx` imports and renders `AboutSection` between TeacherSection and ObjectionsSection
    - `npm run build` completes with zero errors
    - AboutSection visible on /intro with org backstory text and 4 fact tiles
  </done>
</task>

</tasks>

<verification>
Run after all tasks complete:

```bash
cd "/Users/mac/Documents/Art of Living Landing Page" && npm run build
```

Zero build errors confirms all three gaps are closed without breaking existing functionality.

Check rendered /intro page:
- Benefits section shows 3 circular photos (not icon cards)
- Credibility section is dark (bg-gray-950) with meditation image and research narrative
- AboutSection appears after TeacherSection with organizational backstory
</verification>

<success_criteria>
- Gap 14 closed: BenefitsSection renders 3 circular photo cards using breathing-session.jpg, meditation-group.jpg, teacher-guiding.jpg — no lucide-react icon cards
- Gap 16 closed: CredibilitySection is a dark full-bleed 2-column section (bg-gray-950) with image overlay on left and stats + research narrative on right
- Gap 17 closed: AboutSection renders after TeacherSection with 40+ years narrative, Gurudev founding story, volunteer non-profit context, and 4-fact stat tiles
- `npm run build` passes with zero errors
- Page section order: Hero → SessionIntro → Benefits → Credibility → MediaLogos → Testimonials → WhatToExpect → Teacher → About → Objections → Form → FooterCTA
</success_criteria>

<output>
After completion, create `.planning/phases/03-intro-talk-landing-page-trust-content/03-08-SUMMARY.md` following the summary template.
</output>
