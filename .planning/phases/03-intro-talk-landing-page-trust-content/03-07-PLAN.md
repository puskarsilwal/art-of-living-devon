---
phase: 03-intro-talk-landing-page-trust-content
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/intro/hero-section.tsx
  - src/components/intro/session-intro-section.tsx
  - src/components/intro/media-logos-section.tsx
  - src/app/(landing)/intro/page.tsx
autonomous: true
gap_closure: true
requirements:
  - INTRO-06
  - INTRO-07

must_haves:
  truths:
    - "Hero section lists 3 specific bullet points about what participants will learn (SKY breathing, stress/mind management, effortless meditation)"
    - "A 2-column narrative section appears between HeroSection and BenefitsSection explaining what the session is about"
    - "A media/press credibility bar appears after CredibilitySection showing CNN, Yoga Journal, Harvard Health Publishing, and The Washington Post"
  artifacts:
    - path: "src/components/intro/hero-section.tsx"
      provides: "Updated hero with bullet list replacing generic body paragraph"
      contains: "<ul"
    - path: "src/components/intro/session-intro-section.tsx"
      provides: "2-column narrative section: lifestyle photo left, text + bullets right"
      exports: ["SessionIntroSection"]
    - path: "src/components/intro/media-logos-section.tsx"
      provides: "Horizontal press credibility bar with 4 media publication names"
      exports: ["MediaLogosSection"]
    - path: "src/app/(landing)/intro/page.tsx"
      provides: "Wired composition with SessionIntroSection and MediaLogosSection in correct positions"
      contains: "SessionIntroSection"
  key_links:
    - from: "src/app/(landing)/intro/page.tsx"
      to: "src/components/intro/session-intro-section.tsx"
      via: "named import + JSX between HeroSection and BenefitsSection"
      pattern: "SessionIntroSection"
    - from: "src/app/(landing)/intro/page.tsx"
      to: "src/components/intro/media-logos-section.tsx"
      via: "named import + JSX after CredibilitySection"
      pattern: "MediaLogosSection"
    - from: "src/components/intro/session-intro-section.tsx"
      to: "public/images/intro/breathing-session.jpg"
      via: "next/image src prop"
      pattern: "breathing-session.jpg"
---

<objective>
Close gaps 12, 13, and 15 from the human review against the Art of Living reference page.

Gap 12: Hero body copy is one generic sentence. The reference has 3 specific bullet points telling visitors what they will learn. Cold Facebook traffic needs to know precisely what is in the session before they commit.

Gap 13: The page jumps from hero straight to benefits icon cards. The reference has a 2-column "Breathe More, Stress Less" narrative section that explains what the session IS before listing specific benefits. This bridge is essential for cold traffic who have never heard of Art of Living.

Gap 15: No external media/press validation anywhere on the page. The reference shows CNN, Yoga Journal, Harvard Health Publishing, and The Washington Post as a horizontal credibility bar. This is a major missing trust signal.

Purpose: Make the page tell a coherent story — hook (hero with specifics) → explain (session intro narrative) → validate (benefits) → external credibility (media bar) — rather than listing benefits for an organization visitors don't know yet.

Output: Updated hero-section.tsx, new session-intro-section.tsx, new media-logos-section.tsx, updated page.tsx.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-intro-talk-landing-page-trust-content/03-CONTEXT.md
@src/app/(landing)/intro/page.tsx
@src/components/intro/hero-section.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hero generic body paragraph with 3 specific learning bullet points</name>
  <files>src/components/intro/hero-section.tsx</files>
  <action>
Replace the single generic `<p>` body copy paragraph ("Join a free 60-minute intro to the Art of Living. Learn a powerful breathing technique used by millions worldwide.") with a 3-item `<ul>` bullet list that tells visitors exactly what they will learn.

Keep all other elements unchanged: Badge, h1 headline, Clock + date row, Button, and urgency sub-text.

Replace the `<p className="text-white/85 text-base sm:text-lg max-w-xl mx-auto mb-5">` block with:

```tsx
<ul className="text-white/85 text-base sm:text-lg max-w-xl mx-auto mb-5 space-y-2 text-left list-none">
  <li className="flex items-start gap-2">
    <span className="text-primary mt-1 shrink-0">✓</span>
    <span>Learn <strong className="text-white">SKY Breath Meditation</strong> — a science-backed technique used by 500M+ people worldwide</span>
  </li>
  <li className="flex items-start gap-2">
    <span className="text-primary mt-1 shrink-0">✓</span>
    <span>Discover how to <strong className="text-white">manage stress and calm your mind</strong> in minutes, not months</span>
  </li>
  <li className="flex items-start gap-2">
    <span className="text-primary mt-1 shrink-0">✓</span>
    <span>Experience <strong className="text-white">effortless meditation</strong> — even if you've never meditated before</span>
  </li>
</ul>
```

No new imports needed. The `<Clock>` import and `nextIntroTalk` data import remain unchanged. The `Badge`, `Button`, and `Image` imports remain unchanged.
  </action>
  <verify>
    <automated>cd "/Users/mac/Documents/Art of Living Landing Page" && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Open /intro in browser — hero should show 3 checkmarked bullet points below the headline, white text legible on dark image background</manual>
  </verify>
  <done>Hero body copy is 3 specific bullet points (SKY Breath, manage stress, effortless meditation) with checkmark icons, not a single generic sentence. TypeScript compiles with zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create SessionIntroSection — 2-column narrative bridge (photo + description text)</name>
  <files>src/components/intro/session-intro-section.tsx</files>
  <action>
Create a new component file `src/components/intro/session-intro-section.tsx`. This is the "what is this actually?" narrative section that cold Facebook traffic needs before they can engage with specific benefits.

Full component implementation:

```tsx
import Image from "next/image"
import { CheckCircle2 } from "lucide-react"

const sessionPoints = [
  "A guided SKY Breath Meditation experience — the same technique practiced by over 500 million people worldwide",
  "Simple, practical tools to calm the nervous system and clear mental noise — no prior experience needed",
  "An introduction to the Art of Living community and the science behind our breathing practices",
  "Space to ask questions and explore what a regular practice could look like for you",
]

export function SessionIntroSection() {
  return (
    <section className="px-4 py-12 sm:px-6 sm:py-16 lg:py-20 bg-background">
      <div className="max-w-5xl mx-auto">
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-8 lg:gap-12 items-center">
          {/* Left: lifestyle photo */}
          <div className="relative aspect-[4/3] rounded-2xl overflow-hidden shadow-xl order-2 lg:order-1">
            <Image
              src="/images/intro/breathing-session.jpg"
              alt="Participants in a guided breathing session"
              fill
              className="object-cover"
              sizes="(max-width: 1024px) 100vw, 50vw"
            />
            {/* Subtle warm tint overlay to unify with brand palette */}
            <div className="absolute inset-0 bg-primary/10" />
          </div>

          {/* Right: narrative text */}
          <div className="order-1 lg:order-2">
            <p className="text-sm font-semibold text-primary uppercase tracking-wider mb-3">
              What Happens in the Session
            </p>
            <h2 className="text-2xl font-bold tracking-tight sm:text-3xl md:text-4xl mb-4">
              Breathe More,{" "}
              <span className="text-primary">Stress Less</span>
            </h2>
            <p className="text-muted-foreground text-base sm:text-lg leading-relaxed mb-6">
              In 60 minutes, you'll move from wherever you are right now — stressed, curious, or simply looking for something that works — to a place of genuine calm. No philosophy. No sitting in silence for an hour. Just a breathing practice that actually does what it says.
            </p>

            <ul className="space-y-3">
              {sessionPoints.map((point) => (
                <li key={point} className="flex items-start gap-3">
                  <CheckCircle2 className="h-5 w-5 text-primary shrink-0 mt-0.5" />
                  <span className="text-sm sm:text-base text-foreground leading-snug">
                    {point}
                  </span>
                </li>
              ))}
            </ul>
          </div>
        </div>
      </div>
    </section>
  )
}
```

Use `CheckCircle2` from lucide-react (already a project dependency). Use `next/image` with `fill` and `sizes` prop for responsive optimization. The section uses `bg-background` (white) to contrast with the hero's dark image and the benefits section's `bg-muted/50`.
  </action>
  <verify>
    <automated>cd "/Users/mac/Documents/Art of Living Landing Page" && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Section not yet wired into page.tsx — wiring happens in Task 4. TypeScript check is sufficient at this stage.</manual>
  </verify>
  <done>File `src/components/intro/session-intro-section.tsx` exists, exports `SessionIntroSection`, compiles with zero TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 3: Create MediaLogosSection — press credibility bar + wire both new sections into page.tsx</name>
  <files>
    src/components/intro/media-logos-section.tsx
    src/app/(landing)/intro/page.tsx
  </files>
  <action>
**Part A — Create MediaLogosSection**

Create `src/components/intro/media-logos-section.tsx`. This is a horizontal press credibility bar with styled text logos for CNN, Yoga Journal, Harvard Health Publishing, and The Washington Post. Use styled text (not image files) since we don't have brand-licensed logo assets.

```tsx
const mediaLogos = [
  { name: "CNN", style: "font-black tracking-tight text-gray-700" },
  { name: "Yoga Journal", style: "font-semibold italic text-gray-600" },
  { name: "Harvard Health Publishing", style: "font-semibold text-gray-700 text-sm sm:text-base" },
  { name: "The Washington Post", style: "font-serif font-bold text-gray-700" },
]

export function MediaLogosSection() {
  return (
    <section className="px-4 py-8 sm:px-6 sm:py-10 bg-muted/30 border-y border-border/50">
      <div className="max-w-4xl mx-auto">
        <p className="text-center text-xs font-medium text-muted-foreground uppercase tracking-widest mb-5">
          Backed by 100+ peer-reviewed studies &bull; Featured in
        </p>
        <div className="flex flex-wrap items-center justify-center gap-x-8 gap-y-4 sm:gap-x-12">
          {mediaLogos.map((logo) => (
            <span
              key={logo.name}
              className={`text-lg sm:text-xl opacity-60 hover:opacity-90 transition-opacity select-none ${logo.style}`}
            >
              {logo.name}
            </span>
          ))}
        </div>
      </div>
    </section>
  )
}
```

No imports needed beyond the inline data array. Keep it pure JSX — no external image files, no external deps.

**Part B — Wire both new sections into page.tsx**

Update `src/app/(landing)/intro/page.tsx`:

1. Add two new imports at the top (after existing imports):
   ```tsx
   import { SessionIntroSection } from "@/components/intro/session-intro-section"
   import { MediaLogosSection } from "@/components/intro/media-logos-section"
   ```

2. Insert `<SessionIntroSection />` AFTER `<HeroSection />` and BEFORE `<BenefitsSection />`.

3. Insert `<MediaLogosSection />` AFTER `<CredibilitySection />` and BEFORE `<TestimonialsSection />`.

4. Update the inline comment block to reflect the new section order:
   ```
   Trust-building funnel order:
   1. Hero — hook with specific learning bullets
   2. Session Intro — narrative bridge: what is this session?
   3. Benefits — what you'll experience
   4. Credibility — quick stats (500M+ lives, Yale/Harvard research)
   5. Media Logos — external press validation
   6. Testimonials — real people confirm it works
   7. What-to-Expect — reduce uncertainty about the session
   8. Teacher — human connection before asking for registration
   9. Objections — remove last barriers right before the form
   10. Registration — the ask
   11. Footer CTA — final nudge
   ```

Final page.tsx render order:
```tsx
<HeroSection />
<SessionIntroSection />
<BenefitsSection />
<CredibilitySection />
<MediaLogosSection />
<TestimonialsSection />
<WhatToExpectSection />
<TeacherSection />
<ObjectionsSection />
<RegistrationForm />
<FooterCTA />
```
  </action>
  <verify>
    <automated>cd "/Users/mac/Documents/Art of Living Landing Page" && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -20</automated>
    <manual>Open /intro in browser. Scroll from hero: after the hero image section you should see the 2-column "Breathe More, Stress Less" section (photo left, checklist right). After the credibility stats you should see the media logos bar (CNN, Yoga Journal, Harvard Health Publishing, The Washington Post) in muted text on a light background.</manual>
  </verify>
  <done>
    - `src/components/intro/media-logos-section.tsx` exists and exports `MediaLogosSection`
    - `src/app/(landing)/intro/page.tsx` imports and renders `SessionIntroSection` between Hero and Benefits
    - `src/app/(landing)/intro/page.tsx` imports and renders `MediaLogosSection` between Credibility and Testimonials
    - `npm run build` completes with zero errors
    - All 4 media publication names visible on /intro page between credibility section and testimonials
  </done>
</task>

</tasks>

<verification>
Run after all tasks complete:

```bash
cd "/Users/mac/Documents/Art of Living Landing Page" && npm run build
```

Zero build errors confirms all three gaps are closed without breaking the existing page composition.

Check rendered /intro page:
- Hero shows 3 bullet points (SKY Breath, stress/mind management, effortless meditation)
- SessionIntroSection appears immediately after hero with lifestyle photo and narrative text
- MediaLogosSection appears after credibility stats as a press bar with 4 media names
</verification>

<success_criteria>
- Gap 12 closed: Hero body is 3 specific learning bullet points, not one generic sentence
- Gap 13 closed: SessionIntroSection renders between HeroSection and BenefitsSection as a 2-column narrative bridge
- Gap 15 closed: MediaLogosSection renders after CredibilitySection showing CNN, Yoga Journal, Harvard Health Publishing, The Washington Post
- `npm run build` passes with zero errors
- Page section order: Hero → SessionIntro → Benefits → Credibility → MediaLogos → Testimonials → WhatToExpect → Teacher → Objections → Form → FooterCTA
</success_criteria>

<output>
After completion, create `.planning/phases/03-intro-talk-landing-page-trust-content/03-07-SUMMARY.md` following the summary template.
</output>
