---
phase: 03-intro-talk-landing-page-trust-content
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/intro/registration-form.tsx
  - src/components/intro/footer-cta.tsx
  - src/app/globals.css
autonomous: true
gap_closure: true
requirements:
  - INTRO-06
  - INTRO-07
  - INTRO-08
  - INTRO-13
  - INTRO-14

must_haves:
  truths:
    - "Registration form section has warm visual treatment — not a plain card on a plain background; social proof badge near the form reinforces trust at the point of conversion"
    - "Footer CTA section is visually rich — background image with overlay or multi-layer gradient, not a flat solid orange block"
    - "Page has meaningful visual rhythm between sections — not every section is plain white or barely-there bg-muted/50; gradient transitions create a journey feel"
    - "Footer CTA button is visually distinct (different style or urgency framing) from other CTAs on the page"
  artifacts:
    - path: "src/components/intro/registration-form.tsx"
      provides: "Registration form with warm gradient section background, social proof trust badge above form, visual warmth"
      min_lines: 100
    - path: "src/components/intro/footer-cta.tsx"
      provides: "Footer CTA with background image overlay or multi-layer gradient, emotionally compelling visual treatment"
      min_lines: 30
    - path: "src/app/globals.css"
      provides: "Section rhythm utility classes — gradient section backgrounds for visual variety"
      min_lines: 140
  key_links:
    - from: "src/components/intro/registration-form.tsx"
      to: "social proof text (500M+ or trust badge)"
      via: "inline JSX in the section wrapper above the Card"
      pattern: "500M|registrant|trust"
    - from: "src/components/intro/footer-cta.tsx"
      to: "public/images/intro/meditation-group.jpg"
      via: "next/image with fill or CSS background"
      pattern: "meditation-group"
---

<objective>
Transform the Registration Form section, Footer CTA, and overall page visual rhythm from flat/generic into warm, trustworthy, and visually varied — closing the final three visual gaps that make the page feel like a template rather than a premium experience.

Purpose: The registration form is the conversion point — it must feel safe and exciting. The footer CTA is the last chance to convert a hesitant visitor. And without section rhythm, even good individual sections feel like a flat document rather than a journey.

Output: A warm registration form section with social proof, a visually rich footer CTA with background imagery, and global CSS utilities that give the page alternating gradient rhythms.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/intro/registration-form.tsx
@src/components/intro/footer-cta.tsx
@src/app/globals.css
@public/images/intro/meditation-group.jpg
</context>

<tasks>

<task type="auto">
  <name>Task 1: Registration form — warm section background and social proof trust badge</name>
  <files>src/components/intro/registration-form.tsx</files>
  <action>
Enhance `RegistrationForm` to add visual warmth and social proof context around the existing form. Do NOT change form inputs, validation logic, server action, or GDPR checkbox — only wrap with richer visuals.

**Section background (replace bg-muted/50):**
Change the outer `<section>` className from `bg-muted/50` to use a warm gradient:
`bg-gradient-to-b from-primary/5 via-primary/[0.03] to-background`

This creates a warm orange-tinted ambient glow that signals "this matters" without being garish.

**Social proof trust badge above the Card:**
Add a trust indicator row between the section heading area and the Card. Insert directly inside the `max-w-md mx-auto` div, before the `<Card>`:

```tsx
{/* Social proof trust badge */}
<div className="flex items-center justify-center gap-2 mb-6">
  <div className="flex -space-x-2">
    {["C", "P", "S", "R"].map((initial) => (
      <div
        key={initial}
        className="h-8 w-8 rounded-full bg-primary/20 border-2 border-background flex items-center justify-center text-xs font-semibold text-primary"
      >
        {initial}
      </div>
    ))}
  </div>
  <p className="text-sm text-muted-foreground">
    <span className="font-semibold text-foreground">500M+</span> lives transformed worldwide
  </p>
</div>
```

**Card visual upgrade:**
Add to the existing `<Card>` className: `shadow-lg border-primary/10`

This subtle border tint ties the card to the warm background without being heavy-handed.

**Section heading (add above the trust badge):**
Add a heading to give context for the conversion moment — insert before the trust badge div:
```tsx
<div className="text-center mb-6">
  <h2 className="text-xl sm:text-2xl font-bold tracking-tight">
    Ready to Experience It?
  </h2>
  <p className="text-muted-foreground text-sm mt-1">Join your free 60-minute session</p>
</div>
```

**Imports needed:** None new — no new shadcn components required. The overlapping initials are plain divs.

Keep all existing form logic, imports, and structure entirely intact. Only add the section heading, trust badge, and change the section + card className values.
  </action>
  <verify>
    <automated>cd "/Users/mac/Documents/Art of Living Landing Page" && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Open /intro in browser — scroll to registration form: section should have a warm orange-tinted gradient background (not plain gray); overlapping avatar initials and "500M+ lives transformed" text should appear above the form card; card should have subtle primary-tinted border</manual>
  </verify>
  <done>Registration form section has warm gradient background; social proof trust badge with overlapping avatar initials and "500M+" text appears above the Card; Card has subtle primary border; all form functionality intact; TypeScript compiles with zero errors</done>
</task>

<task type="auto">
  <name>Task 2: Footer CTA — background image with overlay and section rhythm CSS utilities</name>
  <files>src/components/intro/footer-cta.tsx, src/app/globals.css</files>
  <action>
**Part A — Footer CTA visual overhaul (footer-cta.tsx):**

Replace the flat `bg-primary` solid block with a layered background that uses `meditation-group.jpg` (already at `public/images/intro/meditation-group.jpg`) as a background image with a deep gradient overlay for emotional pull.

Rewrite `FooterCTA` as:

```tsx
import Image from "next/image"
import { Button } from "@/components/ui/button"
import { Sparkles } from "lucide-react"

export function FooterCTA() {
  return (
    <section className="relative overflow-hidden">
      {/* Background image */}
      <div className="absolute inset-0 z-0">
        <Image
          src="/images/intro/meditation-group.jpg"
          alt=""
          fill
          className="object-cover"
        />
      </div>
      {/* Deep gradient overlay — rich, not flat orange */}
      <div className="absolute inset-0 z-10 bg-gradient-to-br from-primary/90 via-primary/80 to-orange-700/90" />

      {/* Content */}
      <div className="relative z-20 px-4 py-16 sm:px-6 sm:py-20 lg:py-24 text-center text-primary-foreground max-w-4xl mx-auto">
        <div className="flex items-center justify-center gap-2 mb-4">
          <Sparkles className="h-5 w-5 opacity-80" />
          <span className="text-sm font-medium opacity-80 uppercase tracking-wider">Join 500M+ worldwide</span>
          <Sparkles className="h-5 w-5 opacity-80" />
        </div>

        <h2 className="text-2xl font-bold tracking-tight sm:text-3xl md:text-4xl mb-3">
          Your Transformation Starts Here
        </h2>
        <p className="text-base sm:text-lg opacity-90 mb-8 max-w-xl mx-auto">
          One free session. A technique backed by Yale and Harvard research. A community waiting to welcome you.
        </p>

        {/* CTA — ghost/white outline variant for contrast against image background (Gap 9 differentiation) */}
        <a href="#register">
          <Button
            size="lg"
            className="w-full sm:w-auto h-14 text-lg font-semibold px-10 bg-white text-primary hover:bg-white/95 border-2 border-white shadow-lg shadow-black/20"
          >
            Claim Your Free Seat Now
          </Button>
        </a>
        <p className="mt-4 text-sm opacity-70">Free forever • No credit card • Camera optional</p>
      </div>
    </section>
  )
}
```

Note: button text changed to "Claim Your Free Seat Now" (Gap 9 — distinct from "Save My Seat") with the three-line reassurance text below matching the urgency cue pattern from the hero.

**Part B — Section rhythm CSS utilities (globals.css):**

Add section rhythm utility classes at the end of `src/app/globals.css` (after the `@layer base` block) to give sections a more varied visual palette:

```css
@layer utilities {
  /* Section rhythm — graduated background palettes for visual variety */
  .section-warm-gradient {
    background: linear-gradient(to bottom, oklch(1 0 0), oklch(0.98 0.01 60));
  }
  .section-cool-gradient {
    background: linear-gradient(to bottom, oklch(0.97 0.005 260), oklch(1 0 0));
  }
  .section-primary-wash {
    background: linear-gradient(135deg, oklch(0.99 0.005 55), oklch(0.97 0.01 55));
  }
}
```

These utilities are available for future section use. The current plans use inline Tailwind gradient classes (e.g., `bg-gradient-to-b from-primary/5`) to keep changes self-contained within each component file. These CSS utilities document the intent for future phases.
  </action>
  <verify>
    <automated>cd "/Users/mac/Documents/Art of Living Landing Page" && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -10</automated>
    <manual>Open /intro in browser — scroll to footer CTA: should show meditation-group.jpg as background with a rich orange gradient overlay (not flat solid orange); "Your Transformation Starts Here" heading visible; white outline button with "Claim Your Free Seat Now" text; three reassurance items below button. Check globals.css in DevTools — section rhythm utilities are present in the stylesheet.</manual>
  </verify>
  <done>Footer CTA shows meditation-group.jpg background with gradient overlay; heading updated; button text is "Claim Your Free Seat Now" (distinct from other CTAs); reassurance text present; globals.css has section rhythm @layer utilities block; TypeScript and build both pass with zero errors</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx tsc --noEmit` — zero errors
2. `npm run build` — zero errors
3. Open `/intro` in browser end-to-end scroll:
   - Registration form: warm gradient section background, overlapping avatar initials + "500M+" text above card, card with primary/10 border
   - Footer CTA: background image visible under orange gradient overlay, "Your Transformation Starts Here" heading, white "Claim Your Free Seat Now" button, reassurance text below
   - Visual rhythm: Each scroll through the page should feel varied — warm tints, dark sections, light sections — not one flat document
   - CTA variety: Hero (solid primary + urgency sub-text), Benefits (outline border), Form (submit button), Footer (white on image background with different text) — all distinct
</verification>

<success_criteria>
- Registration form: Section background is warm gradient (from-primary/5), social proof trust badge with overlapping initials and "500M+" present above Card, Card has shadow-lg and border-primary/10
- Footer CTA: Uses next/image with fill + gradient overlay (not flat bg-primary), heading is "Your Transformation Starts Here", button is "Claim Your Free Seat Now" in white style
- globals.css: Has @layer utilities block with section rhythm classes
- Zero TypeScript errors
- Zero build errors
- Gaps 8, 9 (footer CTA), 10, and 11 are closed
</success_criteria>

<output>
After completion, create `.planning/phases/03-intro-talk-landing-page-trust-content/03-06-SUMMARY.md`
</output>
