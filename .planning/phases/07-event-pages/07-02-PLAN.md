---
phase: 07-event-pages
plan: "02"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - src/components/events/event-hero-section.tsx
  - src/components/events/event-details-bar.tsx
  - src/components/events/event-experience-section.tsx
  - src/components/events/event-explainer-section.tsx
  - public/images/events/satsang-hero.jpg
autonomous: true
requirements:
  - EVENT-01
  - EVENT-02

must_haves:
  truths:
    - "Hero section renders with full-bleed image, event name, date/time badge, and Register Now CTA linking to external registrationUrl"
    - "Details bar shows 4 icon tiles: Date, Time+Duration, Location, Price — dark background, primary-colored icons"
    - "Experience section conveys the sensory/emotional appeal of attending with pull quote and image"
    - "Explainer section answers 'What is Satsang?' with icon grid or step layout removing attendee uncertainty"
    - "All 4 components are purely presentational — accept EventConfig as prop, render from it, no local state"
  artifacts:
    - path: "src/components/events/event-hero-section.tsx"
      provides: "Full-bleed hero with CTA"
      exports: ["EventHeroSection"]
    - path: "src/components/events/event-details-bar.tsx"
      provides: "4-column icon details strip"
      exports: ["EventDetailsBar"]
    - path: "src/components/events/event-experience-section.tsx"
      provides: "Experiential narrative section"
      exports: ["EventExperienceSection"]
    - path: "src/components/events/event-explainer-section.tsx"
      provides: "Educational what-is-this-event section"
      exports: ["EventExplainerSection"]
    - path: "public/images/events/satsang-hero.jpg"
      provides: "Stock hero image placeholder"
  key_links:
    - from: "event-hero-section.tsx"
      to: "event.registrationUrl"
      via: "<a href={event.registrationUrl} target='_blank'>"
      pattern: "registrationUrl"
    - from: "event-details-bar.tsx"
      to: "EventConfig fields"
      via: "date, time, timezone, duration, location, price"
      pattern: "event\\.(?:date|time|location|price)"
---

<objective>
Build the upper half of the event page template: Hero, Details Bar, Experience, and Explainer sections.

Purpose: These four sections form the critical first scroll — the visitor sees the event, understands the logistics, feels the atmosphere, and understands what they're signing up for. This is where conversion is won or lost.
Output: Four server component files in `src/components/events/` plus the hero stock image.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-event-pages/07-CONTEXT.md
@.planning/phases/07-event-pages/07-RESEARCH.md
@.planning/phases/07-event-pages/07-01-SUMMARY.md

# Established patterns for visual components
@src/components/intro/hero-section.tsx
@src/components/course/hero-section.tsx
@src/components/home/hero-section.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source hero image + build EventHeroSection and EventDetailsBar</name>
  <files>
    public/images/events/satsang-hero.jpg
    src/components/events/event-hero-section.tsx
    src/components/events/event-details-bar.tsx
  </files>
  <action>
**Step 1: Source hero image**
Download a stock meditation/community image from Unsplash to `public/images/events/satsang-hero.jpg`.
Use curl with a direct Unsplash URL with crop params for a landscape 1600x900 image. Example pattern used in this project:
`curl -L "https://images.unsplash.com/photo-{id}?w=1600&h=900&fit=crop&q=80" -o public/images/events/satsang-hero.jpg`
Choose a photo conveying peaceful collective meditation, warm candlelight, or group chanting (search terms: "meditation group", "yoga community", "kirtan chanting", "candlelit meditation"). MUST be a different image from breathing-session.jpg already used on other pages.

**Step 2: EventHeroSection**
Create `src/components/events/event-hero-section.tsx` as a server component (no "use client").

Use the EXACT same full-bleed pattern as `src/components/intro/hero-section.tsx`:
- Outer section: `relative overflow-hidden min-h-[75vh] sm:min-h-[85vh] flex items-center`
- Absolute fill Image with `priority` and `object-cover`
- Gradient overlay div: `absolute inset-0 z-10 bg-gradient-to-b from-black/70 via-black/50 to-black/70`
- Content div: `relative z-20 max-w-4xl mx-auto px-4 sm:px-6 py-20 sm:py-28 text-center w-full`

Content structure inside z-20 div:
1. Badge (shadcn Badge): maps eventType to display label ("Community Satsang" / "Kirtan Evening" / "Community Event"), using `bg-white/20 text-white border-white/30 backdrop-blur-sm` glass style (matches intro hero eyebrow)
2. h1: event.title — `text-4xl sm:text-5xl md:text-6xl font-bold text-white tracking-tight mb-4`
3. If event.subtitle: p with `text-white/80 text-xl sm:text-2xl mb-4 italic`
4. Date/time line: `{event.date} · {event.time} {event.timezone}` — `text-white/70 text-base sm:text-lg mb-10`
5. Register Now CTA: `<a href={event.registrationUrl} target="_blank" rel="noopener noreferrer">` wrapping a shadcn Button with `size="lg"` and `className="h-14 text-lg font-semibold px-10 shadow-lg shadow-primary/30"`. Button text: "Register Now — It's Free" (if price is "Free" or null) or "Register Now" otherwise.

CRITICAL: Use `<a>` not Next.js `<Link>` for external registrationUrl. Link is for internal routes only.

Props: `{ event: EventConfig }` — import EventConfig from `@/lib/data/events`

**Step 3: EventDetailsBar**
Create `src/components/events/event-details-bar.tsx` as a server component.

Dark strip section using `bg-zinc-950 text-white py-8 px-4 sm:px-6`:
- Inner div: `max-w-5xl mx-auto grid grid-cols-2 sm:grid-cols-4 gap-6 sm:gap-8`
- 4 icon tiles: Calendar (date), Clock (time + duration), MapPin (location with optional map link), Ticket (price)
- Each tile: `flex flex-col items-center text-center gap-2`
- Icon: `h-6 w-6 text-primary`
- Label: `text-xs text-white/50 uppercase tracking-widest` (e.g. "DATE", "TIME", "LOCATION", "PRICE")
- Value: `text-sm font-medium` — for time: `{event.time} {event.timezone} · {event.duration}`, for price: `event.price ?? "Contact us"`, for location: if locationMapUrl exists wrap in `<a href={event.locationMapUrl} target="_blank" className="hover:text-primary transition-colors">` otherwise plain text

Import: Calendar, Clock, MapPin, Ticket from lucide-react
  </action>
  <verify>
    <automated>cd "/Users/mac/Documents/Art of Living Landing Page" && ls public/images/events/satsang-hero.jpg && npx tsc --noEmit 2>&1 | grep -E "event-hero|event-details" || echo "Hero and details bar pass type check"</automated>
  </verify>
  <done>Hero image exists at public/images/events/satsang-hero.jpg; EventHeroSection renders with image, badge, h1, date line, and external Register Now CTA; EventDetailsBar renders 4-tile icon grid on dark background</done>
</task>

<task type="auto">
  <name>Task 2: EventExperienceSection and EventExplainerSection</name>
  <files>
    src/components/events/event-experience-section.tsx
    src/components/events/event-explainer-section.tsx
  </files>
  <action>
**EventExperienceSection**
Create `src/components/events/event-experience-section.tsx` as a server component.

Purpose: Emotional/sensory "what it feels like" section. Think: "Close your eyes. Imagine…" — converts curious visitors into believers.

Layout: 2-column on desktop (lg:grid-cols-2 gap-12), single column on mobile. Left: text content. Right: image.

Left column:
- Small eyebrow: `text-primary text-sm font-semibold uppercase tracking-widest mb-3` — "The Experience"
- h2: `text-3xl sm:text-4xl font-bold text-gray-900 mb-6 leading-tight` — use `event.title` in heading, e.g. "What Does {event.title} Feel Like?"
- Pull quote blockquote: `border-l-4 border-primary pl-6 mb-6` — use first testimonial quote from event.testimonials if present, otherwise use a static evocative placeholder: "Ninety minutes that felt like a moment — and I carried that stillness with me for days."
- Body paragraph: use event.longDescription if present, otherwise derive atmospheric copy about Satsang from event.description — 2-3 sentences describing the experience of meditation, chant, and collective silence
- CTA button (secondary/outline): "Join Us — {event.date}" linking to `event.registrationUrl` as `<a>` with target="_blank"

Right column:
- `relative h-64 sm:h-80 lg:h-full min-h-[300px] rounded-2xl overflow-hidden`
- next/image fill with object-cover
- Use event.heroImage as src (same hero image works fine here — different crop/context)
- Subtle gradient overlay at bottom: `absolute bottom-0 left-0 right-0 h-1/3 bg-gradient-to-t from-black/30 to-transparent`

Section bg: `bg-white py-16 sm:py-24 px-4 sm:px-6`
Inner container: `max-w-6xl mx-auto`

**EventExplainerSection**
Create `src/components/events/event-explainer-section.tsx` as a server component.

Purpose: Educational — removes uncertainty about "what is Satsang?" and "what will I be doing?" Converts skeptical visitors into confident registrants.

Layout: Centered header + icon feature grid (3 columns on desktop, 1 on mobile)

Header:
- Eyebrow: "What to Expect" — `text-primary text-sm font-semibold uppercase tracking-widest mb-3`
- h2: "An Evening of Peace and Connection" (for satsang/kirtan) — adapt based on eventType. Use static copy appropriate per eventType:
  - satsang: "An Evening of Peace and Connection"
  - kirtan: "An Evening of Chant and Joy"
  - default: "A Community Gathering"
- Subheading: `text-gray-600 max-w-2xl mx-auto text-lg` — short 1-sentence description of the event's spiritual/wellness purpose

Feature grid (3 items) — use shadcn Card or plain divs with `rounded-xl border bg-muted/30 p-6`:
For satsang eventType, use:
  1. Icon: Music2 (lucide-react) — Title: "Kirtan Chanting" — Body: "Ancient Sanskrit mantras sung in call-and-response. No experience needed — the melodies are simple and the rhythm carries you."
  2. Icon: Wind (lucide-react) — Title: "Guided Meditation" — Body: "A short guided breathing and meditation practice. You'll leave with a technique you can use at home."
  3. Icon: Users (lucide-react) — Title: "Collective Silence" — Body: "The power of meditating with others is palpable. Shared silence creates an atmosphere that's hard to describe and unforgettable once felt."

For kirtan eventType, swap to music-focused items. For other types, use generic community gathering items.

Icon style: `h-8 w-8 text-primary mb-4`
Feature title: `font-semibold text-gray-900 mb-2`
Feature body: `text-gray-600 text-sm leading-relaxed`

Section bg: `bg-muted/30 py-16 sm:py-24 px-4 sm:px-6`
  </action>
  <verify>
    <automated>cd "/Users/mac/Documents/Art of Living Landing Page" && npx tsc --noEmit 2>&1 | grep -E "event-experience|event-explainer" || echo "Experience and explainer sections pass type check"</automated>
  </verify>
  <done>EventExperienceSection renders 2-col layout with pull quote, body copy, and external CTA; EventExplainerSection renders centered header with 3-feature icon grid appropriate to eventType; both accept EventConfig prop; no TypeScript errors</done>
</task>

</tasks>

<verification>
Run `cd "/Users/mac/Documents/Art of Living Landing Page" && npx tsc --noEmit` — zero errors in all 4 new component files.
Check `public/images/events/satsang-hero.jpg` exists and has file size > 50KB (real image, not placeholder).
</verification>

<success_criteria>
- Hero image downloaded to public/images/events/satsang-hero.jpg
- EventHeroSection: full-bleed image, glass badge, h1, date line, external Register Now CTA
- EventDetailsBar: dark strip with 4 icon tiles (date/time/location/price) from EventConfig
- EventExperienceSection: 2-col layout with pull quote, body copy, external CTA
- EventExplainerSection: 3-feature icon grid explaining what attendees will experience
- All components are server components (no "use client")
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-event-pages/07-02-SUMMARY.md`
</output>
