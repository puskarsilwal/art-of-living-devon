---
phase: 06-homepage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/home/about-section.tsx
  - src/components/home/local-guides-section.tsx
  - src/components/home/footer-cta.tsx
autonomous: true
requirements:
  - HOME-03
  - HOME-04

must_haves:
  truths:
    - "AboutSection shows credibility stats strip (500M+, 180+, 100+, Yale & Harvard) from existing data module"
    - "AboutSection shows one-sentence mission statement and brief Gurudev name/title mention"
    - "LocalGuidesSection shows teacher/host photo with 'Your local guides' framing and Devon/Southwest copy"
    - "FooterCta is a full-bleed warm-colored section with a final 'Register for a Free Intro Talk' CTA"
  artifacts:
    - path: "src/components/home/about-section.tsx"
      provides: "Credibility stats strip + mission statement + Gurudev mention"
      min_lines: 45
    - path: "src/components/home/local-guides-section.tsx"
      provides: "Teacher/host photo with local framing and descriptive copy"
      min_lines: 40
    - path: "src/components/home/footer-cta.tsx"
      provides: "Full-bleed footer CTA section linking to intro talk registration"
      min_lines: 25
  key_links:
    - from: "src/components/home/about-section.tsx"
      to: "src/lib/data/credibility-stats.ts"
      via: "import credibilityStats"
      pattern: "credibilityStats"
    - from: "src/components/home/local-guides-section.tsx"
      to: "/images/intro/teacher-guiding.jpg"
      via: "next/image src prop"
      pattern: "teacher-guiding\\.jpg"
    - from: "src/components/home/footer-cta.tsx"
      to: "/intro"
      via: "Link href"
      pattern: "href=\"/intro\""
---

<objective>
Build the AboutSection, LocalGuidesSection, and FooterCta homepage components — the lower-page sections that establish credibility, local identity, and close with a final conversion CTA.

Purpose: HOME-03 requires a brief "About Art of Living" section with credibility facts. HOME-04 requires blended Art of Living brand colors with Devon/Southwest local warmth. The footer CTA reinforces the primary conversion action before the page ends.

Output: Three new components in src/components/home/. All are pure server components importing from existing data modules and image assets.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-homepage/06-CONTEXT.md
@.planning/phases/06-homepage/06-RESEARCH.md
@src/lib/data/credibility-stats.ts
@src/components/intro/credibility-section.tsx
@src/components/course/founder-section.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build AboutSection component</name>
  <files>src/components/home/about-section.tsx</files>
  <action>
Create `src/components/home/about-section.tsx` — credibility stats strip + mission statement + Gurudev mention.

CRITICAL: Import credibilityStats from the existing data module at `@/lib/data/credibility-stats` — do NOT inline the stats. The module already exports the four stats: 500M+, 180+, 100+, and Yale & Harvard.

Structure:
- Section wrapper: `<section className="py-16 sm:py-20 bg-muted/30">`
- Section header: `<h2>About Art of Living</h2>` with a subline
- Mission statement paragraph: "Founded in 1981 by Sri Sri Ravi Shankar, Art of Living is a global non-profit dedicated to stress-free, violence-free societies through breath-based practices, yoga, and service."
- Gurudev mention: "Our teacher and founder, Sri Sri Ravi Shankar (affectionately known as Gurudev), has guided over 500 million people across 180 countries."
- Stats grid: map over `credibilityStats` to render a 2x2 (mobile) or 4-column (desktop) grid of stat cards
  - Each stat card: `<div className="text-center">` with `<p className="text-3xl font-bold text-primary">` for the number and `<p className="text-sm text-muted-foreground mt-1">` for the label
  - Grid: `<div className="grid grid-cols-2 lg:grid-cols-4 gap-6 mt-10">`
- Background use `about-art-of-living.webp` is NOT needed here (Gurudev photo optional per CONTEXT.md) — text-only presentation for Gurudev mention

Imports:
```tsx
import { credibilityStats } from "@/lib/data/credibility-stats"
```

Named export `AboutSection`. Pure server component, no "use client".

Note: The `credibilityStats` type may have an optional `isText` field for "Yale & Harvard" — render it the same way (the text is already styled correctly without special-casing).
  </action>
  <verify>
    <automated>cd "/Users/mac/Documents/Art of Living Landing Page" && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Confirm about-section.tsx imports credibilityStats (not inlined), renders 4 stat cards, and includes mission statement text.</manual>
  </verify>
  <done>src/components/home/about-section.tsx exists, compiles cleanly, imports credibilityStats from @/lib/data/credibility-stats, renders 4 stat cards in a responsive grid, includes mission statement and Gurudev mention. No TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Build LocalGuidesSection and FooterCta components</name>
  <files>
    src/components/home/local-guides-section.tsx
    src/components/home/footer-cta.tsx
  </files>
  <action>
Create two components:

**A. src/components/home/local-guides-section.tsx**

A warm, human section with a teacher/host photo and local Devon/Southwest framing. Per CONTEXT.md: reuse teacher photo from the intro page; use "Your local guides" framing without a specific name (Phase 3-11 removed the named TeacherSection).

Structure:
- Section wrapper: `<section className="py-16 sm:py-20 bg-background">`
- 2-column layout on desktop: image left, text right (`<div className="grid md:grid-cols-2 gap-12 items-center">`)
- Image column: `<div className="relative h-72 md:h-96 rounded-2xl overflow-hidden">` with `<Image src="/images/intro/teacher-guiding.jpg" alt="Art of Living teacher guiding meditation" fill className="object-cover" />`
- Text column:
  - Eyebrow: `<p className="text-primary font-semibold text-sm uppercase tracking-wide mb-3">Your Local Guides</p>`
  - Heading: `<h2 className="text-3xl font-bold mb-4">Certified Art of Living Teachers</h2>`
  - Body copy: "Our teachers are certified by the Art of Living Foundation after rigorous training. They bring warmth, wisdom, and personal experience to every session across Devon and Southwest England."
  - Secondary line: "All practices taught exactly as developed by Sri Sri Ravi Shankar — authentic, safe, and profoundly effective."
  - CTA link: `<Link href="/intro"><Button variant="outline" className="mt-6">Meet Us at an Intro Talk</Button></Link>`

Imports: `Image` from `"next/image"`, `Link` from `"next/link"`, `Button` from `"@/components/ui/button"`.
Named export `LocalGuidesSection`. Pure server component.

---

**B. src/components/home/footer-cta.tsx**

A full-bleed warm-colored closing section with a single conversion CTA. Matches the pattern of `FooterCta` in the course page (Phase 05-04).

Structure:
- Section wrapper with primary orange background: `<section className="py-16 sm:py-20 bg-primary">`
- Centered content: headline, subline, CTA button using secondary variant for white-on-orange contrast
- Headline: `<h2 className="text-3xl font-bold text-white sm:text-4xl">Ready to Experience the Difference?</h2>`
- Subline: `<p className="text-white/90 mt-4 text-lg max-w-xl mx-auto">Join a free 60-minute intro talk in Devon or Southwest England. No experience needed.</p>`
- CTA: `<Link href="/intro"><Button size="lg" variant="secondary" className="mt-8 text-base px-8">Register for a Free Intro Talk</Button></Link>`
- Wrapper div: `<div className="text-center max-w-2xl mx-auto px-4">`
- Container: `<div className="container mx-auto px-4 sm:px-6">`

Imports: `Link` from `"next/link"`, `Button` from `"@/components/ui/button"`.
Named export `FooterCta`. Pure server component.
  </action>
  <verify>
    <automated>cd "/Users/mac/Documents/Art of Living Landing Page" && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Confirm both files exist in src/components/home/ with correct named exports and image paths.</manual>
  </verify>
  <done>Both src/components/home/local-guides-section.tsx and src/components/home/footer-cta.tsx exist, compile cleanly. local-guides uses /images/intro/teacher-guiding.jpg with next/image fill. footer-cta has bg-primary section with white text and secondary variant CTA button linking to /intro. No TypeScript errors.</done>
</task>

</tasks>

<verification>
After all tasks: `npx tsc --noEmit` passes with zero errors. All three files exist in src/components/home/ with named exports matching what the 06-04 assembler will import.
</verification>

<success_criteria>
- src/components/home/about-section.tsx: imports credibilityStats, 4-stat responsive grid, mission statement, Gurudev mention
- src/components/home/local-guides-section.tsx: 2-column layout, teacher-guiding.jpg with next/image, local Devon/Southwest copy
- src/components/home/footer-cta.tsx: full-bleed bg-primary section, white text, secondary variant CTA button to /intro
- All three compile cleanly as pure server components
</success_criteria>

<output>
After completion, create `.planning/phases/06-homepage/06-03-SUMMARY.md`
</output>
