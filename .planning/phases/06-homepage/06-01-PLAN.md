---
phase: 06-homepage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/site-header.tsx
  - src/components/layout/site-footer.tsx
autonomous: true
requirements:
  - HOME-02
  - HOME-04

must_haves:
  truths:
    - "SiteHeader shows logo + nav links (Intro Talk, Part 1 Course, Events) on desktop"
    - "SiteHeader shows a primary 'Register Free' CTA button that links to /intro"
    - "SiteFooter includes nav links to Intro Talk and Part 1 Course alongside Privacy Policy"
    - "Both components apply brand-consistent styling matching existing pages"
  artifacts:
    - path: "src/components/layout/site-header.tsx"
      provides: "Sticky header with logo, desktop nav links, and register CTA button"
      min_lines: 35
    - path: "src/components/layout/site-footer.tsx"
      provides: "Footer with copyright, privacy policy, and quick navigation links"
      min_lines: 30
  key_links:
    - from: "src/components/layout/site-header.tsx"
      to: "/intro"
      via: "next/link href"
      pattern: "href=\"/intro\""
    - from: "src/components/layout/site-header.tsx"
      to: "/happiness-program"
      via: "next/link href"
      pattern: "href=\"/happiness-program\""
    - from: "src/components/layout/site-header.tsx"
      to: "/events"
      via: "next/link href with TODO comment"
      pattern: "href=\"/events\""
---

<objective>
Upgrade SiteHeader and SiteFooter with full navigation links so the (main) route group has a proper nav bar and footer for all its pages (homepage and privacy policy).

Purpose: HOME-02 requires the homepage to link to the intro talk page, course page, and upcoming events. The SiteHeader is the primary navigation mechanism for the (main) route group and currently has only a logo — no nav links or CTA button.

Output: Updated site-header.tsx with sticky nav bar (logo + desktop nav links + register CTA), updated site-footer.tsx with page links alongside privacy policy. No new dependencies needed.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-homepage/06-CONTEXT.md
@.planning/phases/06-homepage/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade SiteHeader with nav links and register CTA</name>
  <files>src/components/layout/site-header.tsx</files>
  <action>
Replace the current minimal SiteHeader (logo only, 19 lines) with a full navigation header. The current file has only the logo link — no nav links, no CTA button.

Implementation:
- Keep the existing sticky header shell: `sticky top-0 z-50 w-full border-b border-border/40 bg-background/95 backdrop-blur supports-[backdrop-filter]:bg-background/60`
- Change the inner flex container to `justify-between` (currently no justify — logo is left-aligned by default)
- Left: existing logo link (orange circle + "Art of Living Devon & Southwest" text) — keep exactly as-is
- Center/Right: `<nav className="hidden md:flex items-center gap-6 text-sm font-medium">` containing three Link elements:
  - `<Link href="/intro" className="text-muted-foreground hover:text-foreground transition-colors">Intro Talk</Link>`
  - `<Link href="/happiness-program" className="text-muted-foreground hover:text-foreground transition-colors">Part 1 Course</Link>`
  - `<Link href="/events" className="text-muted-foreground hover:text-foreground transition-colors">Events</Link>` — add comment `{/* /events route created in Phase 7 */}`
- Right: primary CTA button — `<Link href="/intro" className="hidden md:block"><Button size="sm">Register Free</Button></Link>`
- Import: add `import { Button } from "@/components/ui/button"` alongside existing `import Link from "next/link"`
- Mobile: the nav and CTA are `hidden md:flex` / `hidden md:block` — mobile users navigate via hero CTAs and offering cards on the homepage. Add comment: `{/* TODO: mobile hamburger nav — Phase 7+ enhancement */}`
- Named export `SiteHeader` (no change to export name)
  </action>
  <verify>
    <automated>cd "/Users/mac/Documents/Art of Living Landing Page" && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Check that site-header.tsx renders logo on left, three nav links in center (desktop only), and Register Free button on right — visible on any (main) page.</manual>
  </verify>
  <done>site-header.tsx compiles with no TypeScript errors; nav links for Intro Talk, Part 1 Course, Events, and Register Free CTA are present in the JSX; all links use next/link with correct hrefs.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade SiteFooter with navigation links</name>
  <files>src/components/layout/site-footer.tsx</files>
  <action>
Extend the current SiteFooter to include quick navigation links for the main pages. Current footer is copyright + privacy policy link only.

Implementation:
- Keep the existing outer `<footer>` shell and brand-consistent styling (`border-t border-border/40 bg-secondary/30`)
- Change the inner flex to a 3-column layout on desktop:
  - Left col: `<p>&copy; {new Date().getFullYear()} Art of Living Devon &amp; Southwest</p>` (unchanged)
  - Center col: `<nav className="flex gap-4 text-sm">` with links:
    - `<Link href="/intro">Intro Talk</Link>`
    - `<Link href="/happiness-program">Part 1 Course</Link>`
    - `<Link href="/events">Events</Link>` with comment `{/* Phase 7 */}`
    - Each link: `className="underline-offset-4 hover:text-foreground hover:underline"`
  - Right col: existing privacy policy link (unchanged)
- Mobile layout: `flex-col items-center gap-2 text-center` (all three stacked), desktop: `md:flex-row md:justify-between md:text-left`
- No new imports needed — `Link` already imported
- Named export `SiteFooter` (no change)
  </action>
  <verify>
    <automated>cd "/Users/mac/Documents/Art of Living Landing Page" && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Verify footer shows copyright left, nav links center, privacy policy right on desktop. Stacked on mobile.</manual>
  </verify>
  <done>site-footer.tsx compiles with no TypeScript errors; nav links for Intro Talk, Part 1 Course, Events, and Privacy Policy are present; layout matches brand styling.</done>
</task>

</tasks>

<verification>
After both tasks: run `npm run build` in the project root. Build must succeed with zero errors. The SiteHeader and SiteFooter are shared across (main) pages — any breakage affects homepage and privacy-policy page.
</verification>

<success_criteria>
- SiteHeader: logo left, desktop nav (Intro Talk, Part 1 Course, Events), Register Free CTA button — all hidden on mobile
- SiteFooter: 3-column desktop layout — copyright | page nav links | Privacy Policy
- TypeScript compiles cleanly; `npm run build` passes
- No new npm dependencies added
</success_criteria>

<output>
After completion, create `.planning/phases/06-homepage/06-01-SUMMARY.md`
</output>
