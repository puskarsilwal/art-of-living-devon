---
phase: 06-homepage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/home/hero-section.tsx
  - src/components/home/offerings-section.tsx
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-04

must_haves:
  truths:
    - "Hero section shows full-bleed background image with dark gradient overlay and headline"
    - "Hero headline communicates Devon/Southwest community identity"
    - "Hero has two CTAs: primary 'Register for a Free Intro Talk' and secondary 'Explore Our Courses'"
    - "Offerings section shows three scannable cards: Intro Talk, Part 1 Course, Community Events"
    - "Each offering card has icon, 1-2 line description, and CTA button linking to correct page"
  artifacts:
    - path: "src/components/home/hero-section.tsx"
      provides: "Full-bleed hero with next/image background, gradient overlay, headline, two CTAs"
      min_lines: 40
    - path: "src/components/home/offerings-section.tsx"
      provides: "Three-column offering cards grid with icons, descriptions, and CTA buttons"
      min_lines: 60
  key_links:
    - from: "src/components/home/hero-section.tsx"
      to: "/images/intro/breathing-session.jpg"
      via: "next/image src prop"
      pattern: "breathing-session\\.jpg"
    - from: "src/components/home/offerings-section.tsx"
      to: "/intro"
      via: "Link href in offering card"
      pattern: "href=\"/intro\""
    - from: "src/components/home/offerings-section.tsx"
      to: "/happiness-program"
      via: "Link href in offering card"
      pattern: "href=\"/happiness-program\""
    - from: "src/components/home/offerings-section.tsx"
      to: "/events"
      via: "Link href in offering card (Phase 7 forward-compatible)"
      pattern: "href=\"/events\""
---

<objective>
Build the HeroSection and OfferingsSection homepage components — the two above-fold sections that establish the page's identity and route visitors to the right offering.

Purpose: These are the highest-impact sections. The hero creates the first impression and drives the primary CTA (Register for Free Intro Talk). The offerings section routes visitors to all three offerings with equal visual weight. Together they satisfy HOME-01 (branded community hub), HOME-02 (links to all pages), and HOME-04 (brand visual feel).

Output: Two new components in src/components/home/ — hero-section.tsx and offerings-section.tsx. Both are pure server components. No client state needed.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-homepage/06-CONTEXT.md
@.planning/phases/06-homepage/06-RESEARCH.md
@src/components/intro/hero-section.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build HeroSection component</name>
  <files>src/components/home/hero-section.tsx</files>
  <action>
Create `src/components/home/hero-section.tsx` — a new file (the `home/` directory will be created alongside it).

Pattern: mirrors `src/components/intro/hero-section.tsx` (Phase 3) — full-bleed section with absolute-positioned next/image background, dark gradient overlay div, and content z-stacked on top.

Structure:
```tsx
"use server" // not needed — omit; server component by default in App Router

import Image from "next/image"
import Link from "next/link"
import { Button } from "@/components/ui/button"

export function HeroSection() {
  return (
    <section className="relative overflow-hidden min-h-[65vh] sm:min-h-[75vh] flex items-center">
      {/* Background image — use /images/intro/breathing-session.jpg (hero quality) */}
      <div className="absolute inset-0 z-0">
        <Image
          src="/images/intro/breathing-session.jpg"
          alt=""
          fill
          className="object-cover object-center"
          priority
        />
      </div>
      {/* Gradient overlay — dark to allow white text readability */}
      <div className="absolute inset-0 z-10 bg-gradient-to-b from-black/70 via-black/55 to-black/65" />
      {/* Content */}
      <div className="relative z-20 max-w-4xl mx-auto px-4 sm:px-6 py-16 sm:py-24 text-center w-full">
        {/* Eyebrow badge */}
        <div className="inline-flex items-center gap-2 rounded-full bg-white/20 backdrop-blur-sm px-4 py-1.5 mb-6">
          <span className="text-sm font-medium text-white">Devon &amp; Southwest</span>
        </div>
        {/* Headline */}
        <h1 className="text-4xl font-bold tracking-tight text-white sm:text-5xl md:text-6xl mb-4">
          Breathe. Meditate.<br />
          <span className="text-primary">Transform.</span>
        </h1>
        {/* Subheadline */}
        <p className="text-lg text-white/85 mb-8 max-w-2xl mx-auto">
          Join your local Art of Living community in Devon and Southwest England.
          Free intro talks, science-backed meditation courses, and community events.
        </p>
        {/* CTAs */}
        <div className="flex flex-col gap-4 sm:flex-row sm:justify-center">
          <Link href="/intro">
            <Button size="lg" className="w-full sm:w-auto text-base px-8">
              Register for a Free Intro Talk
            </Button>
          </Link>
          <Link href="/happiness-program">
            <Button
              size="lg"
              variant="outline"
              className="w-full sm:w-auto text-base px-8 border-white/70 text-white hover:bg-white/10 hover:text-white"
            >
              Explore Our Courses
            </Button>
          </Link>
        </div>
      </div>
    </section>
  )
}
```

Named export `HeroSection`. No `"use client"` — pure server component. No animations or complex client logic.
  </action>
  <verify>
    <automated>cd "/Users/mac/Documents/Art of Living Landing Page" && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Confirm hero-section.tsx file exists in src/components/home/ with the correct named export.</manual>
  </verify>
  <done>src/components/home/hero-section.tsx exists, compiles cleanly, exports HeroSection function; contains next/image with breathing-session.jpg, gradient overlay, headline mentioning Devon/Southwest, and two CTA buttons linking to /intro and /happiness-program.</done>
</task>

<task type="auto">
  <name>Task 2: Build OfferingsSection component</name>
  <files>src/components/home/offerings-section.tsx</files>
  <action>
Create `src/components/home/offerings-section.tsx` — three-column card grid showing the three offerings: Intro Talk, Part 1 Course, Community Events.

Pattern: uses shadcn Card, Button from existing UI library; lucide-react icons for visual variety. Pure server component.

Offerings data (inline in component — no separate data module needed for 3 static items):
```
- Intro Talk: icon Wind, title "Free Intro Talk", href "/intro", cta "Register Free"
  desc: "Experience SKY Breath Meditation in a live 60-minute session. No experience needed. Completely free."
- Part 1 Course: icon BookOpen, title "Part 1 Course", href "/happiness-program", cta "Explore the Course"
  desc: "Three days to learn Sudarshan Kriya, pranayama, and yoga. Science-backed and life-changing."
- Community Events: icon CalendarDays, title "Community Events", href "/events", cta "See Events"
  desc: "Satsang, kirtan, and meditation gatherings across Devon and Southwest England."
  (add comment: {/* /events route created in Phase 7 */})
```

Structure:
- Section wrapper: `<section className="py-16 sm:py-20 bg-background">`
- Section header: `<h2>` "What We Offer" with a subline "Find the right practice for you"
- Grid: `<div className="grid grid-cols-1 md:grid-cols-3 gap-6 mt-12">`
- Each card: shadcn `<Card className="flex flex-col h-full hover:shadow-lg transition-shadow">` with:
  - `<CardHeader>`: icon in a colored circle (`rounded-full bg-primary/10 p-3 w-fit`) + `<CardTitle className="text-xl mt-4">`
  - `<CardContent className="flex-1">`: description text
  - `<div className="p-6 pt-0">` (card footer area): `<Link href={offering.href}><Button className="w-full">{cta}</Button></Link>`

Imports needed:
```tsx
import Link from "next/link"
import { Wind, BookOpen, CalendarDays } from "lucide-react"
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
import { Button } from "@/components/ui/button"
```

Named export `OfferingsSection`. No `"use client"`.
  </action>
  <verify>
    <automated>cd "/Users/mac/Documents/Art of Living Landing Page" && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Confirm offerings-section.tsx file exists with three card entries and correct hrefs for /intro, /happiness-program, /events.</manual>
  </verify>
  <done>src/components/home/offerings-section.tsx exists, compiles cleanly, exports OfferingsSection function; contains three offering cards for Intro Talk (/intro), Part 1 Course (/happiness-program), and Community Events (/events with Phase 7 comment).</done>
</task>

</tasks>

<verification>
After both tasks: `npx tsc --noEmit` passes with zero errors. Both files exist in `src/components/home/`. Named exports match expected imports in the assembler plan (06-04).
</verification>

<success_criteria>
- src/components/home/hero-section.tsx: full-bleed hero, breathing-session.jpg background, Devon/Southwest headline, two CTAs
- src/components/home/offerings-section.tsx: three offering cards with icons, descriptions, CTA buttons, correct hrefs
- TypeScript compiles cleanly
- Pure server components — no "use client" directives
</success_criteria>

<output>
After completion, create `.planning/phases/06-homepage/06-02-SUMMARY.md`
</output>
